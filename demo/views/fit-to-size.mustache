<style>
    /* The "stage" is the resizable frame whose width the slider controls; the fit
       row inside it is width:100%, so narrowing the stage narrows the row and the
       Fit engine's ResizeObserver re-settles it. Capped at 100% so it never
       exceeds the viewport — on a phone it's already at the narrow end. */
    .fit-stage {
        max-width: 100%;
        margin-block: 0.75rem;
        border: 1px dashed var(--pc-border-color);
        border-radius: 8px;
        transition: max-width 0.08s ease;
    }

    .fit-slider-row {
        display: flex;
        align-items: center;
        gap: 0.75rem;
        margin-top: 1rem;
        font-size: var(--pc-font-size-sm, 1.4rem);
        color: var(--pc-text-color-2);
    }
    .fit-slider-row input[type="range"] { flex: 1; min-width: 0; }
    .fit-slider-row output { font-variant-numeric: tabular-nums; min-width: 4.5ch; text-align: end; }

    /* Fit row (example 1): a plain flex row. Children don't shrink (flex:0 0 auto)
       + never wrap — so the engine folds them instead of the browser squishing. */
    .fit-toolbar {
        display: flex;
        align-items: center;
        gap: 0.5rem;
        width: 100%;
    }
    .fit-toolbar > * { flex: 0 0 auto; white-space: nowrap; }
    /* Keep each step's icon + label on ONE line, so the engine folds the button
       rather than the button wrapping its own content when cramped. */
    .fit-toolbar [data-pc-fit-step] {
        display: inline-flex;
        align-items: center;
        gap: 0.4rem;
        white-space: nowrap;
    }

    /* Example 2 uses a CSS CONTAINER QUERY (not the fit engine): the card is the
       query container and its own width swaps the chart for compact KPIs. These
       are the only non-component styles here — every visible piece is a real pa-*. */
    /* NB: pure-admin's root font-size is 10px, so 1rem = 10px — 45rem = 450px. */
    .fit-cq { container-type: inline-size; }
    .fit-cq__chart { width: 20rem; height: 10rem; }
    .fit-cq__chart { display: none; }              /* narrow default → show the KPIs */
    .fit-cq__kpi { display: flex; flex-direction: column; align-items: flex-start; gap: 0.6rem; }
    @container (min-width: 45rem) {
        .fit-cq__chart { display: block; }         /* wide (≥450px) → show the chart */
        .fit-cq__kpi { display: none; }
    }

    /* Example 3 uses ONE CSS container query that restyles the card on
       SEVERAL axes at three widths — panels grid → tabs → icon-tabs, plus a
       header that sheds supplier / packaging / trend. Demo-only layout classes
       are prefixed .prod; every visible piece (pa-badge, pa-stat, pa-tabs,
       pa-dot-leaders) is a real component. Root font 10px → 1rem = 10px. */
    .prod { container-type: inline-size; }

    .prod__header {
        display: flex; justify-content: space-between; align-items: flex-start; gap: 1.5rem;
    }
    .prod__title { margin: 0.4rem 0 0.3rem; }
    .prod__meta { color: var(--pc-text-color-2); font-size: var(--pc-font-size-sm, 1.4rem); }
    .prod__price { flex: 0 0 auto; text-align: end; }
    .prod__price-num { font-size: 2rem; font-weight: 700; font-variant-numeric: tabular-nums; }
    .prod__price-trend { margin-top: 0.4rem; }

    /* Header detail + trend badge start hidden; they appear as the card widens. */
    .prod__meta-pkg, .prod__meta-sup, .prod__price-trend { display: none; }

    /* DEFAULT (narrowest): tabbed, one panel at a time, tabs are ICON-ONLY. */
    .prod__tabs { margin-top: 1.2rem; }
    /* Re-assert the tab item's flex row + gap. The buttons carry .pa-tooltip
       (for the icon-only width), and .pa-tooltip's display:inline-block would
       otherwise clobber .pa-tabs__item's own display:flex — collapsing the gap
       so the icon touches the label. This two-class selector wins. */
    .prod__tabs .pa-tabs__item { display: flex; align-items: center; justify-content: center; gap: 0.5rem; }
    .prod__tabs .pa-tabs__item span { display: none; }   /* icon-only until there's room */
    .prod__panels { margin-top: 1.2rem; }
    .prod__panel { display: none; }
    .prod__panel--active { display: block; }
    .prod__panel-title { display: none; }                 /* the tab label already names it */

    /* MEDIUM (≥34rem / 340px): tab TEXT appears; header regains packaging + trend. */
    @container (min-width: 34rem) {
        .prod__tabs .pa-tabs__item span { display: inline; }
        .prod__meta-pkg { display: inline; }
        .prod__price-trend { display: inline-flex; }
    }

    /* WIDE (≥64rem / 640px): drop the tabs, lay the three panels side-by-side,
       reveal the per-panel titles and the supplier line. */
    @container (min-width: 64rem) {
        .prod__tabs { display: none; }
        .prod__panels { display: grid; grid-template-columns: repeat(3, 1fr); gap: 1.6rem; }
        .prod__panel { display: block; }
        .prod__panel + .prod__panel {
            padding-inline-start: 1.6rem;
            border-inline-start: 1px solid var(--pc-border-color);
        }
        .prod__panel-title { display: block; }
        .prod__meta-sup { display: inline; }
    }

    /* Example 4 — the SAME multi-level product card as example 3, but driven by
       the Container Breakpoint engine instead of a CSS @container. NOTE: .prodx
       does NOT set container-type, so no @container fires here — the swaps are
       driven by the engine: data-pc-show toggles the shared .d-none (a class you
       can watch in devtools), and [data-mode="grid"] does the one real 2-D layout
       change. Swappable pieces are VISIBLE by default; the engine hides the
       out-of-mode ones. Structural rules below mirror example 3's .prod. */
    .prodx__header { display: flex; justify-content: space-between; align-items: flex-start; gap: 1.5rem; }
    .prodx__title { margin: 0.4rem 0 0.3rem; }
    .prodx__meta { color: var(--pc-text-color-2); font-size: var(--pc-font-size-sm, 1.4rem); }
    .prodx__price { flex: 0 0 auto; text-align: end; }
    .prodx__price-num { font-size: 2rem; font-weight: 700; font-variant-numeric: tabular-nums; }
    .prodx__chart { height: 8rem; margin-top: 1.2rem; }
    .prodx__tabs { margin-top: 1.2rem; }
    /* flex-gap fix: .pa-tooltip's display:inline-block would clobber the tab item's
       flex, colliding the icon and label (see example 3). */
    .prodx__tabs .pa-tabs__item { display: flex; align-items: center; justify-content: center; gap: 0.5rem; }
    .prodx__panels { margin-top: 1.2rem; }
    /* tabs / icons modes: one panel at a time (standard tab pattern) */
    .prodx__panel { display: none; }
    .prodx__panel--active { display: block; }
    /* grid mode: the one genuine 2-D layout change — three panels side-by-side */
    .prodx[data-mode="grid"] .prodx__panels { display: grid; grid-template-columns: repeat(3, 1fr); gap: 1.6rem; }
    .prodx[data-mode="grid"] .prodx__panel { display: block; }
    .prodx[data-mode="grid"] .prodx__panel + .prodx__panel { padding-inline-start: 1.6rem; border-inline-start: 1px solid var(--pc-border-color); }
    .ex4-log {
        margin-top: 0.8rem; max-height: 9rem; overflow: auto;
        font-size: var(--pc-font-size-xs, 1.2rem);
        font-family: var(--pc-font-family-mono, ui-monospace, monospace);
        color: var(--pc-text-color-2);
    }
    .ex4-log > div { padding: 0.15rem 0; border-bottom: 1px dashed var(--pc-border-color); }
</style>

<p>
    The Fit engine (<code>fit.js</code>) shrinks a horizontal row to fit its
    container — degrading the least-important slots first, lowest priority first,
    and restoring them as space returns. It powers the navbar, but it's not
    navbar-only: call <code>pureAdmin.components.fit.init(el)</code> on any flex
    row (a toolbar, a filter bar, a card's action cluster).
</p>

<div class="pa-callout pa-callout--info mb-4">
    <strong>The vocabulary.</strong>
    <code>data-pc-fit="hide|steps|sidebar"</code> makes an element a slot;
    <code>data-pc-fit-priority</code> orders them (lower goes first);
    <code>data-pc-fit-auto</code> on a container folds in <em>every</em> child
    without tagging each; <code>data-pc-fit-ignore</code> pins one out entirely;
    and <code>pureAdmin.config.fit.defaultPriority</code> (or a container's
    <code>data-pc-fit-default-priority</code>) sets the fallback rank.
    <strong>Drag the sliders</strong> — or just open this page on a narrow phone —
    to watch each row fold.
</div>

<!-- ============================================================
     EXAMPLE 1 — Card toolbar: shrink, don't lose (steps + ignore)
     ============================================================ -->
<h3 class="mt-4 mb-2">1 · Card toolbar — shrink, don't lose</h3>
<p>
    A <code>steps</code> slot degrades <strong>full label → icon-only → gone</strong>
    instead of vanishing outright, so an action stays reachable as an icon before
    it's dropped. Here <strong>Save</strong> is pinned full
    (<code>data-pc-fit-ignore</code>); <strong>Duplicate</strong> and
    <strong>Export</strong> shrink to icons (higher priority survives longer);
    <strong>Delete</strong> is un-tagged, so it inherits the toolbar's
    <code>data-pc-fit-default-priority="20"</code> and drops first.
</p>

<div class="fit-slider-row">
    <span>Container width</span>
    <input type="range" min="200" max="720" value="360"
           oninput="fitSetWidth('stage1', this.value)" aria-label="Example 1 width">
    <output id="stage1-w">360px</output>
</div>

<div class="fit-stage" id="stage1" style="max-width: 360px">
    <div class="pa-card" style="margin: 0">
        <div class="pa-card__header">
            <div class="pa-card__title"><span class="pa-card__title-text">Sales Overview</span></div>
        </div>
        <div class="pa-card__body">
            <div id="ex1-toolbar" class="fit-toolbar" data-pc-fit-auto data-pc-fit-default-priority="20">
                <button class="pa-btn pa-btn--primary" data-pc-fit-ignore>
                    <span class="pa-btn__icon"><svg xmlns="http://www.w3.org/2000/svg" width="1em" height="1em" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M15.2 3a2 2 0 0 1 1.4.6l3.8 3.8a2 2 0 0 1 .6 1.4V19a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2z"/><path d="M17 21v-7a1 1 0 0 0-1-1H8a1 1 0 0 0-1 1v7"/><path d="M7 3v4a1 1 0 0 0 1 1h7"/></svg></span> Save
                </button>
                <button class="pa-btn" data-pc-fit="steps" data-pc-fit-priority="40">
                    <span data-pc-fit-step="0"><span class="pa-btn__icon"><svg xmlns="http://www.w3.org/2000/svg" width="1em" height="1em" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect width="14" height="14" x="8" y="8" rx="2" ry="2"/><path d="M4 16c-1.1 0-2-.9-2-2V4c0-1.1.9-2 2-2h10c1.1 0 2 .9 2 2"/></svg></span> Duplicate</span>
                    <span data-pc-fit-step="1" class="pc-fit-hidden"><span class="pa-btn__icon"><svg xmlns="http://www.w3.org/2000/svg" width="1em" height="1em" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect width="14" height="14" x="8" y="8" rx="2" ry="2"/><path d="M4 16c-1.1 0-2-.9-2-2V4c0-1.1.9-2 2-2h10c1.1 0 2 .9 2 2"/></svg></span></span>
                </button>
                <button class="pa-btn" data-pc-fit="steps" data-pc-fit-priority="30">
                    <span data-pc-fit-step="0"><span class="pa-btn__icon"><svg xmlns="http://www.w3.org/2000/svg" width="1em" height="1em" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M12 15V3"/><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><path d="m7 10 5 5 5-5"/></svg></span> Export</span>
                    <span data-pc-fit-step="1" class="pc-fit-hidden"><span class="pa-btn__icon"><svg xmlns="http://www.w3.org/2000/svg" width="1em" height="1em" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M12 15V3"/><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><path d="m7 10 5 5 5-5"/></svg></span></span>
                </button>
                <button class="pa-btn">
                    <span class="pa-btn__icon"><svg xmlns="http://www.w3.org/2000/svg" width="1em" height="1em" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M10 11v6"/><path d="M14 11v6"/><path d="M19 6v14a2 2 0 0 1-2 2H7a2 2 0 0 1-2-2V6"/><path d="M3 6h18"/><path d="M8 6V4a2 2 0 0 1 2-2h4a2 2 0 0 1 2 2v2"/></svg></span> Delete
                </button>
            </div>
            <p class="mt-3 mb-0" style="color: var(--pc-text-color-2); font-size: var(--pc-font-size-sm, 1.4rem)">
                Drag left: Delete drops → Export shrinks to its icon, then drops →
                Duplicate shrinks → only Save remains, always full.
            </p>
        </div>
    </div>
</div>

<!-- ============================================================
     EXAMPLE 2 — Product card: chart ↔ KPI via a CSS container query
     ============================================================ -->
<h3 class="mt-5 mb-2">2 · Product card — chart ↔ KPI</h3>
<p>
    A different job needs a different tool. Swapping a rich <strong>chart</strong>
    for compact <strong>KPI numbers</strong> is a 2-D layout change, not a 1-D row
    fold — so this one is a pure CSS <strong>container query</strong> on the card,
    no JS engine. Left half stays product info (category, title, code); the right
    half shows the Chart.js sparkline while there's room and swaps to a
    <code>pa-stat</code> KPI once the card narrows past <code>45rem</code> (450px). Every
    visible piece — <code>pa-card</code>, <code>pa-badge</code>, <code>pa-stat</code>,
    the chart — is a real component.
</p>

<div class="fit-slider-row">
    <span>Container width</span>
    <input type="range" min="260" max="720" value="640"
           oninput="fitSetWidth('stage2', this.value)" aria-label="Example 2 width">
    <output id="stage2-w">640px</output>
</div>

<div class="fit-stage" id="stage2" style="max-width: 640px; border: 0">
    <div class="pa-card fit-cq" style="margin: 0">
        <div class="pa-card__body d-flex" style="align-items: center; gap: 1.5rem">
            <div class="flex-1" style="min-width: 0">
                <span class="pa-badge pa-badge--color-3">Beverages</span>
                <h4 class="mt-2 mb-1">Arabica Cold Brew</h4>
                <div style="color: var(--pc-text-color-2); font-size: var(--pc-font-size-sm, 1.4rem)">
                    <code>SKU-4471</code> · 1 L bottle
                </div>
            </div>
            <div style="flex: 0 0 auto">
                <div class="fit-cq__chart"><canvas id="ex2-chart"></canvas></div>
                <div class="fit-cq__kpi">
                    <div class="pa-stat">
                        <div class="pa-stat__icon pa-stat__icon--success"><svg xmlns="http://www.w3.org/2000/svg" width="1em" height="1em" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M16 7h6v6"/><path d="m22 7-8.5 8.5-5-5L2 17"/></svg></div>
                        <div class="pa-stat__content">
                            <div class="pa-stat__number">$847K</div>
                            <div class="pa-stat__label">Revenue</div>
                        </div>
                    </div>
                    <span class="pa-badge pa-badge--success">▲ 12.5%</span>
                </div>
            </div>
        </div>
    </div>
</div>
<p class="mt-2" style="color: var(--pc-text-color-2); font-size: var(--pc-font-size-sm, 1.4rem)">
    Drag left (or view on a phone): past <code>45rem</code> the chart gives way to
    the KPI stat — same data, denser. Drag right to bring the chart back.
</p>

<h4 class="mt-4 mb-2">Same card, no slider — resize your browser window</h4>
<p style="color: var(--pc-text-color-2); font-size: var(--pc-font-size-sm, 1.4rem)">
    Identical card, but with <strong>no <code>.fit-stage</code> wrapper and no
    slider</strong> — it's just full-width in the page. The container query reads
    the card's <em>real</em> width, so drag your browser window narrower and the
    chart becomes the KPI on its own. (The sidebar eats ~250px, so the card hits
    the 450px breakpoint while the window is still ~730px.) This is the production
    behaviour; the slider above only simulated it.
</p>
<div class="pa-card fit-cq" style="margin: 0">
    <div class="pa-card__body d-flex" style="align-items: center; gap: 1.5rem">
        <div class="flex-1" style="min-width: 0">
            <span class="pa-badge pa-badge--color-3">Beverages</span>
            <h4 class="mt-2 mb-1">Arabica Cold Brew</h4>
            <div style="color: var(--pc-text-color-2); font-size: var(--pc-font-size-sm, 1.4rem)">
                <code>SKU-4471</code> · 1 L bottle
            </div>
        </div>
        <div style="flex: 0 0 auto">
            <div class="fit-cq__chart"><canvas id="ex2b-chart"></canvas></div>
            <div class="fit-cq__kpi">
                <div class="pa-stat">
                    <div class="pa-stat__icon pa-stat__icon--success"><svg xmlns="http://www.w3.org/2000/svg" width="1em" height="1em" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M16 7h6v6"/><path d="m22 7-8.5 8.5-5-5L2 17"/></svg></div>
                    <div class="pa-stat__content">
                        <div class="pa-stat__number">$847K</div>
                        <div class="pa-stat__label">Revenue</div>
                    </div>
                </div>
                <span class="pa-badge pa-badge--success">▲ 12.5%</span>
            </div>
        </div>
    </div>
</div>

<!-- ============================================================
     EXAMPLE 3 — Rich product card: multi-level degrade (one CQ, 3 widths)
     ============================================================ -->
<h3 class="mt-5 mb-2">3 · Rich product card — degrade on multiple levels</h3>
<p>
    The real power shows when one card restyles on <strong>several axes at once</strong>.
    This product card is a single CSS <strong>container query</strong> with three
    widths. As it narrows, the three data panels
    (<strong>Orders · Stock · Sales</strong>) collapse from a
    <strong>3-column grid</strong> into <strong>tabs</strong>, then the tab labels
    drop to <strong>icons only</strong>; in step, the header sheds its supplier,
    then its packaging line and trend badge. Every visible piece —
    <code>pa-badge</code>, <code>pa-stat</code>, <code>pa-tabs</code>,
    <code>pa-dot-leaders</code> — is a real component; only the layout switches.
</p>

<div class="fit-slider-row">
    <span>Container width</span>
    <input type="range" min="260" max="880" value="760"
           oninput="fitSetWidth('stage3', this.value)" aria-label="Example 3 width">
    <output id="stage3-w">760px</output>
</div>

<div class="fit-stage" id="stage3" style="max-width: 760px; border: 0">
    <div class="pa-card prod" style="margin: 0">
        <div class="pa-card__body">

            <!-- Header: identity + price — both shed detail as the card narrows -->
            <div class="prod__header">
                <div style="min-width: 0">
                    <span class="pa-badge pa-badge--color-3">Beverages</span>
                    <h4 class="prod__title">Arabica Cold Brew</h4>
                    <div class="prod__meta"><code>SKU-4471</code><span class="prod__meta-pkg"> · 1 L bottle · 12 per case</span><span class="prod__meta-sup"> · Yirgacheffe Co-op</span></div>
                </div>
                <div class="prod__price">
                    <div class="prod__price-num">$4.80</div>
                    <span class="pa-badge pa-badge--success prod__price-trend">▲ 12.5%</span>
                </div>
            </div>

            <!-- Tab strip: only rendered in the tabbed (narrow / medium) form.
                 Icon-only until the card is wide enough for the labels. -->
            <div class="pa-tabs pa-tabs--full prod__tabs">
                <button class="pa-tabs__item pa-tabs__item--active pa-tooltip" data-tooltip="Orders" data-panel="orders" onclick="prodTab(this)">
                    <i class="fa-solid fa-cart-shopping"></i><span>Orders</span>
                </button>
                <button class="pa-tabs__item pa-tooltip" data-tooltip="Stock" data-panel="stock" onclick="prodTab(this)">
                    <i class="fa-solid fa-boxes-stacked"></i><span>Stock</span>
                </button>
                <button class="pa-tabs__item pa-tooltip" data-tooltip="Sales" data-panel="sales" onclick="prodTab(this)">
                    <i class="fa-solid fa-chart-line"></i><span>Sales</span>
                </button>
            </div>

            <!-- Panels: a 3-col grid when wide; one-at-a-time under the tabs when narrow -->
            <div class="prod__panels">
                <div class="prod__panel prod__panel--active" data-panel="orders">
                    <h5 class="prod__panel-title mb-1">Orders</h5>
                    <div class="pa-stat">
                        <div class="pa-stat__icon pa-stat__icon--primary"><svg xmlns="http://www.w3.org/2000/svg" width="1em" height="1em" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M11 21.73a2 2 0 0 0 2 0l7-4A2 2 0 0 0 21 16V8a2 2 0 0 0-1-1.73l-7-4a2 2 0 0 0-2 0l-7 4A2 2 0 0 0 3 8v8a2 2 0 0 0 1 1.73z"/><path d="M12 22V12"/><polyline points="3.29 7 12 12 20.71 7"/><path d="m7.5 4.27 9 5.15"/></svg></div>
                        <div class="pa-stat__content">
                            <div class="pa-stat__number">1,204</div>
                            <div class="pa-stat__label">Orders · 30d</div>
                        </div>
                    </div>
                    <div class="pa-dot-leaders mt-2">
                        <div class="pa-dot-leaders__item"><span class="pa-dot-leaders__label">Open</span><span class="pa-dot-leaders__leader"></span><span class="pa-dot-leaders__value">38</span></div>
                        <div class="pa-dot-leaders__item"><span class="pa-dot-leaders__label">Fulfilled</span><span class="pa-dot-leaders__leader"></span><span class="pa-dot-leaders__value">1,142</span></div>
                        <div class="pa-dot-leaders__item"><span class="pa-dot-leaders__label">Returned</span><span class="pa-dot-leaders__leader"></span><span class="pa-dot-leaders__value">24</span></div>
                    </div>
                </div>
                <div class="prod__panel" data-panel="stock">
                    <h5 class="prod__panel-title mb-1">Stock</h5>
                    <div class="pa-stat">
                        <div class="pa-stat__icon pa-stat__icon--warning"><svg xmlns="http://www.w3.org/2000/svg" width="1em" height="1em" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M15 21v-5a1 1 0 0 0-1-1h-4a1 1 0 0 0-1 1v5"/><path d="M17.774 10.31a1.12 1.12 0 0 0-1.549 0 2.5 2.5 0 0 1-3.451 0 1.12 1.12 0 0 0-1.548 0 2.5 2.5 0 0 1-3.452 0 1.12 1.12 0 0 0-1.549 0 2.5 2.5 0 0 1-3.77-3.248l2.889-4.184A2 2 0 0 1 7 2h10a2 2 0 0 1 1.653.873l2.895 4.192a2.5 2.5 0 0 1-3.774 3.244"/><path d="M4 10.95V19a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2v-8.05"/></svg></div>
                        <div class="pa-stat__content">
                            <div class="pa-stat__number">8,450</div>
                            <div class="pa-stat__label">On hand</div>
                        </div>
                    </div>
                    <div class="pa-dot-leaders mt-2">
                        <div class="pa-dot-leaders__item"><span class="pa-dot-leaders__label">Reserved</span><span class="pa-dot-leaders__leader"></span><span class="pa-dot-leaders__value">1,120</span></div>
                        <div class="pa-dot-leaders__item"><span class="pa-dot-leaders__label">Available</span><span class="pa-dot-leaders__leader"></span><span class="pa-dot-leaders__value">7,330</span></div>
                        <div class="pa-dot-leaders__item"><span class="pa-dot-leaders__label">Reorder at</span><span class="pa-dot-leaders__leader"></span><span class="pa-dot-leaders__value">2,000</span></div>
                    </div>
                </div>
                <div class="prod__panel" data-panel="sales">
                    <h5 class="prod__panel-title mb-1">Sales</h5>
                    <div class="pa-stat">
                        <div class="pa-stat__icon pa-stat__icon--success"><svg xmlns="http://www.w3.org/2000/svg" width="1em" height="1em" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"/><path d="M16 8h-6a2 2 0 1 0 0 4h4a2 2 0 1 1 0 4H8"/><path d="M12 18V6"/></svg></div>
                        <div class="pa-stat__content">
                            <div class="pa-stat__number">$847K</div>
                            <div class="pa-stat__label">Revenue · 30d</div>
                        </div>
                    </div>
                    <div class="pa-dot-leaders mt-2">
                        <div class="pa-dot-leaders__item"><span class="pa-dot-leaders__label">Units</span><span class="pa-dot-leaders__leader"></span><span class="pa-dot-leaders__value">12.4k</span></div>
                        <div class="pa-dot-leaders__item"><span class="pa-dot-leaders__label">Avg order</span><span class="pa-dot-leaders__leader"></span><span class="pa-dot-leaders__value">$68.30</span></div>
                        <div class="pa-dot-leaders__item"><span class="pa-dot-leaders__label">Margin</span><span class="pa-dot-leaders__leader"></span><span class="pa-dot-leaders__value">41%</span></div>
                    </div>
                </div>
            </div>

        </div>
    </div>
</div>
<p class="mt-2" style="color: var(--pc-text-color-2); font-size: var(--pc-font-size-sm, 1.4rem)">
    Three levels, one query. <strong>≥640px</strong> → the 3-panel grid;
    <strong>340–640px</strong> → tabbed with labels; <strong>&lt;340px</strong> →
    icon-only tabs. Drag slowly and watch the header drop its supplier, packaging,
    then trend badge at the matching steps — nothing is re-fetched or re-rendered,
    the same markup just re-flows.
</p>

<!-- ============================================================
     EXAMPLE 4 — Same multi-level card as #3, but ENGINE-driven
     ============================================================ -->
<h3 class="mt-5 mb-2">4 · Same card, on the engine — with a chart it builds on demand</h3>
<p>
    This is example 3's product card again — the same three-level degrade
    (<strong>3-panel grid → tabs → icon tabs</strong>) with the same header
    trimming — but driven by the <strong>Container Breakpoint engine</strong>
    instead of a CSS <code>@container</code>. Why bother, if CSS already did it?
    Two things CSS can't: the engine toggles a <strong>real <code>.d-none</code>
    class</strong> you can watch hop in devtools (not an invisible computed
    <code>display:none</code>), and it fires an <strong>event</strong> — so the
    grid-only <strong>revenue chart</strong> is <em>built only when the card is
    wide enough to show it, and destroyed otherwise</em>. Watch the log.
</p>
<p style="color: var(--pc-text-color-2); font-size: var(--pc-font-size-sm, 1.4rem)">
    <code>data-pc-breakpoints='{"icons":0,"tabs":34,"grid":64}'</code> names the
    three modes; each swappable piece carries <code>data-pc-show="grid"</code> /
    <code>"grid tabs"</code> / <code>"tabs"</code> and the engine toggles
    <code>.d-none</code> on the ones out of mode. <code>[data-mode="grid"]</code>
    still does the one thing that's a real layout change rather than a show/hide —
    the 2-D panels grid — and the <code>pc:breakpoint</code> event drives the
    chart's build / destroy. Every visible piece is a real <code>pa-*</code> component.
</p>

<div class="pa-callout pa-callout--info mb-3">
    <strong>What the numbers mean.</strong> The values in
    <code>data-pc-breakpoints</code> are <strong>rem</strong> — minimum widths of
    the <em>card itself</em> (its content-box inline size), not the viewport and
    not percentages. pure-admin's root font-size is <code>10px</code>, so
    <code>34</code> = 340px and <code>64</code> = 640px — the same numbers as the
    <code>@container</code> breakpoints in example 3. The engine picks the
    <strong>largest</strong> mode whose width the card has passed (700px → grid,
    500px → tabs). Prefer pixels? Add
    <code>data-pc-breakpoint-unit="px"</code> and write
    <code>{"icons":0,"tabs":340,"grid":640}</code> — identical behaviour.
    Percent isn't a unit here on purpose: "does this layout fit" is an
    absolute-pixel question (label widths, gaps, padding); for viewport-relative
    rules reach for a CSS media query instead.
</div>

<div class="fit-slider-row">
    <span>Container width</span>
    <input type="range" min="260" max="880" value="760"
           oninput="fitSetWidth('stage4', this.value)" aria-label="Example 4 width">
    <output id="stage4-w">760px</output>
</div>

<div class="fit-stage" id="stage4" style="max-width: 760px; border: 0">
    <div class="pa-card prodx" id="ex4" style="margin: 0"
         data-pc-breakpoints='{"icons":0,"tabs":34,"grid":64}' data-pc-breakpoint-initial="tabs">
        <div class="pa-card__body">

            <!-- Header — sheds supplier (grid only), then packaging + trend (grid/tabs) -->
            <div class="prodx__header">
                <div style="min-width: 0">
                    <span class="pa-badge pa-badge--color-3">Beverages</span>
                    <h4 class="prodx__title">Arabica Cold Brew</h4>
                    <div class="prodx__meta"><code>SKU-4471</code><span data-pc-show="grid tabs"> · 1 L bottle · 12 per case</span><span class="d-none" data-pc-show="grid"> · Yirgacheffe Co-op</span></div>
                </div>
                <div class="prodx__price">
                    <div class="prodx__price-num">$4.80</div>
                    <span class="pa-badge pa-badge--success" data-pc-show="grid tabs">▲ 12.5%</span>
                </div>
            </div>

            <!-- Hero revenue chart — ONLY in grid mode. The engine's pc:breakpoint
                 event builds the Chart.js instance on entering grid and destroys it
                 on leaving (mount on demand). Pre-stamped d-none to match initial="tabs". -->
            <div class="prodx__chart d-none" data-pc-show="grid"><canvas id="ex4-chart"></canvas></div>

            <!-- Tab strip — only in tabs/icons; labels only in tabs (icons → icon-only) -->
            <div class="pa-tabs pa-tabs--full prodx__tabs" data-pc-show="tabs icons">
                <button class="pa-tabs__item pa-tabs__item--active pa-tooltip" data-tooltip="Orders" data-panel="orders" onclick="prodxTab(this)">
                    <i class="fa-solid fa-cart-shopping"></i><span data-pc-show="tabs">Orders</span>
                </button>
                <button class="pa-tabs__item pa-tooltip" data-tooltip="Stock" data-panel="stock" onclick="prodxTab(this)">
                    <i class="fa-solid fa-boxes-stacked"></i><span data-pc-show="tabs">Stock</span>
                </button>
                <button class="pa-tabs__item pa-tooltip" data-tooltip="Sales" data-panel="sales" onclick="prodxTab(this)">
                    <i class="fa-solid fa-chart-line"></i><span data-pc-show="tabs">Sales</span>
                </button>
            </div>

            <!-- Panels — 3-col grid in grid mode; one-at-a-time under the tabs otherwise -->
            <div class="prodx__panels">
                <div class="prodx__panel prodx__panel--active" data-panel="orders">
                    <h5 class="prodx__panel-title mb-1 d-none" data-pc-show="grid">Orders</h5>
                    <div class="pa-stat">
                        <div class="pa-stat__icon pa-stat__icon--primary"><svg xmlns="http://www.w3.org/2000/svg" width="1em" height="1em" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M11 21.73a2 2 0 0 0 2 0l7-4A2 2 0 0 0 21 16V8a2 2 0 0 0-1-1.73l-7-4a2 2 0 0 0-2 0l-7 4A2 2 0 0 0 3 8v8a2 2 0 0 0 1 1.73z"/><path d="M12 22V12"/><polyline points="3.29 7 12 12 20.71 7"/><path d="m7.5 4.27 9 5.15"/></svg></div>
                        <div class="pa-stat__content">
                            <div class="pa-stat__number">1,204</div>
                            <div class="pa-stat__label">Orders · 30d</div>
                        </div>
                    </div>
                    <div class="pa-dot-leaders mt-2">
                        <div class="pa-dot-leaders__item"><span class="pa-dot-leaders__label">Open</span><span class="pa-dot-leaders__leader"></span><span class="pa-dot-leaders__value">38</span></div>
                        <div class="pa-dot-leaders__item"><span class="pa-dot-leaders__label">Fulfilled</span><span class="pa-dot-leaders__leader"></span><span class="pa-dot-leaders__value">1,142</span></div>
                        <div class="pa-dot-leaders__item"><span class="pa-dot-leaders__label">Returned</span><span class="pa-dot-leaders__leader"></span><span class="pa-dot-leaders__value">24</span></div>
                    </div>
                </div>
                <div class="prodx__panel" data-panel="stock">
                    <h5 class="prodx__panel-title mb-1 d-none" data-pc-show="grid">Stock</h5>
                    <div class="pa-stat">
                        <div class="pa-stat__icon pa-stat__icon--warning"><svg xmlns="http://www.w3.org/2000/svg" width="1em" height="1em" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M15 21v-5a1 1 0 0 0-1-1h-4a1 1 0 0 0-1 1v5"/><path d="M17.774 10.31a1.12 1.12 0 0 0-1.549 0 2.5 2.5 0 0 1-3.451 0 1.12 1.12 0 0 0-1.548 0 2.5 2.5 0 0 1-3.452 0 1.12 1.12 0 0 0-1.549 0 2.5 2.5 0 0 1-3.77-3.248l2.889-4.184A2 2 0 0 1 7 2h10a2 2 0 0 1 1.653.873l2.895 4.192a2.5 2.5 0 0 1-3.774 3.244"/><path d="M4 10.95V19a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2v-8.05"/></svg></div>
                        <div class="pa-stat__content">
                            <div class="pa-stat__number">8,450</div>
                            <div class="pa-stat__label">On hand</div>
                        </div>
                    </div>
                    <div class="pa-dot-leaders mt-2">
                        <div class="pa-dot-leaders__item"><span class="pa-dot-leaders__label">Reserved</span><span class="pa-dot-leaders__leader"></span><span class="pa-dot-leaders__value">1,120</span></div>
                        <div class="pa-dot-leaders__item"><span class="pa-dot-leaders__label">Available</span><span class="pa-dot-leaders__leader"></span><span class="pa-dot-leaders__value">7,330</span></div>
                        <div class="pa-dot-leaders__item"><span class="pa-dot-leaders__label">Reorder at</span><span class="pa-dot-leaders__leader"></span><span class="pa-dot-leaders__value">2,000</span></div>
                    </div>
                </div>
                <div class="prodx__panel" data-panel="sales">
                    <h5 class="prodx__panel-title mb-1 d-none" data-pc-show="grid">Sales</h5>
                    <div class="pa-stat">
                        <div class="pa-stat__icon pa-stat__icon--success"><svg xmlns="http://www.w3.org/2000/svg" width="1em" height="1em" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"/><path d="M16 8h-6a2 2 0 1 0 0 4h4a2 2 0 1 1 0 4H8"/><path d="M12 18V6"/></svg></div>
                        <div class="pa-stat__content">
                            <div class="pa-stat__number">$847K</div>
                            <div class="pa-stat__label">Revenue · 30d</div>
                        </div>
                    </div>
                    <div class="pa-dot-leaders mt-2">
                        <div class="pa-dot-leaders__item"><span class="pa-dot-leaders__label">Units</span><span class="pa-dot-leaders__leader"></span><span class="pa-dot-leaders__value">12.4k</span></div>
                        <div class="pa-dot-leaders__item"><span class="pa-dot-leaders__label">Avg order</span><span class="pa-dot-leaders__leader"></span><span class="pa-dot-leaders__value">$68.30</span></div>
                        <div class="pa-dot-leaders__item"><span class="pa-dot-leaders__label">Margin</span><span class="pa-dot-leaders__leader"></span><span class="pa-dot-leaders__value">41%</span></div>
                    </div>
                </div>
            </div>

        </div>
    </div>
</div>
<div class="ex4-log" id="ex4-log" aria-live="polite"></div>

<div class="pa-callout pa-callout--warning mt-4">
    <strong>Right tool check.</strong> Two techniques, two jobs. The fit
    <em>engine</em> (example 1) shrinks a 1-D <strong>row</strong> of items —
    <code>steps</code> to keep something reachable, <code>hide</code> for
    supplementary bits. A <strong>container query</strong> (example 2) is for a
    2-D <strong>layout swap</strong> — chart ↔ KPI, sidebar ↔ drawer, and
    (example 3) several swaps stacked into one query: panels → tabs → icons plus
    header trimming, all keyed off the card's own width. And when the hidden
    branch is <strong>expensive</strong> (a chart, a date-picker, a shadow-DOM
    widget), reach past CSS for the <strong>Container Breakpoint engine</strong>
    (example 4): same width thresholds, but it emits an event so JS can
    <em>build the widget only when its mode is on screen and tear it down
    otherwise</em> — the piece a Svelte/LiveView wrapper turns into real
    mount/unmount. None of these is a form-reflow engine: for fields wrapping to
    new lines, use the grid / flex-wrap.
</div>

<script>
    (function () {
        function fitInit(id) {
            var el = document.getElementById(id);
            if (el && window.pureAdmin && pureAdmin.components && pureAdmin.components.fit) {
                pureAdmin.components.fit.init(el);
            }
        }
        fitInit('ex1-toolbar');   // examples 2 & 3 are pure CSS container queries — no JS engine

        // Example 3 tab switching (only visible in the tabbed / narrow form; the
        // wide 3-panel grid ignores it since every panel shows). Scoped to the
        // clicked card so multiple product cards wouldn't cross-talk.
        window.prodTab = function (btn) {
            var card = btn.closest('.prod');
            if (!card) return;
            var panel = btn.getAttribute('data-panel');
            card.querySelectorAll('.prod__tabs .pa-tabs__item').forEach(function (t) {
                t.classList.toggle('pa-tabs__item--active', t === btn);
            });
            card.querySelectorAll('.prod__panel').forEach(function (p) {
                p.classList.toggle('prod__panel--active', p.getAttribute('data-panel') === panel);
            });
        };

        // Slider → stage width. Example 1's fit row (width:100% of the stage) is
        // re-settled by the engine's ResizeObserver; example 2's card is the
        // container-query container, so its own width drives the chart ↔ KPI swap.
        window.fitSetWidth = function (stageId, w) {
            var stage = document.getElementById(stageId);
            if (stage) stage.style.maxWidth = w + 'px';
            var out = document.getElementById(stageId + '-w');
            if (out) out.textContent = w + 'px';
        };

        // Example 2's Chart.js sparkline. Created while the card is wide (chart
        // visible) so it renders at a real size; Chart.js's own ResizeObserver
        // re-fits it when the container query shows it again after a narrow spell.
        if (window.Chart) {
            var accent = (getComputedStyle(document.documentElement)
                .getPropertyValue('--pc-accent') || '#3b82f6').trim();
            // Both cards (slider-driven and window-driven) get the same sparkline.
            ['ex2-chart', 'ex2b-chart'].forEach(function (id) {
                var canvas = document.getElementById(id);
                if (!canvas) return;
                new Chart(canvas, {
                    type: 'line',
                    data: {
                        labels: ['Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun'],
                        datasets: [{
                            data: [612, 640, 606, 701, 760, 803, 847],
                            borderColor: accent,
                            backgroundColor: 'rgba(59, 130, 246, 0.12)',
                            fill: true, tension: 0.4, pointRadius: 0, borderWidth: 2
                        }]
                    },
                    options: {
                        responsive: true, maintainAspectRatio: false,
                        plugins: { legend: { display: false }, tooltip: { enabled: false } },
                        scales: { x: { display: false }, y: { display: false } }
                    }
                });
            });
        }
    })();
</script>

<script>
    // Example 4 — the engine-driven twin of example 3. container-breakpoint.js
    // auto-wires the card (data-pc-breakpoints), toggles .d-none on the
    // data-pc-show pieces, reflects [data-mode], and fires 'pc:breakpoint'. We use
    // that event to BUILD the hero revenue chart only in 'grid' mode and DESTROY
    // it otherwise — the "mount on demand" a Svelte {#if} would automate — and to
    // switch tabs. prodxTab handles clicks in the tabbed (narrow) modes.
    (function () {
        var card = document.getElementById('ex4');
        if (!card) return;
        var logEl = document.getElementById('ex4-log');
        var chart = null, builds = 0;

        function log(msg) {
            if (!logEl) return;
            var line = document.createElement('div');
            line.textContent = msg;
            logEl.insertBefore(line, logEl.firstChild);
        }

        function buildChart() {
            var canvas = document.getElementById('ex4-chart');
            if (!canvas || !window.Chart) return null;
            var accent = (getComputedStyle(document.documentElement)
                .getPropertyValue('--pc-accent') || '#3b82f6').trim();
            return new Chart(canvas, {
                type: 'line',
                data: {
                    labels: ['Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun'],
                    datasets: [{
                        data: [612, 640, 606, 701, 760, 803, 847],
                        borderColor: accent, backgroundColor: 'rgba(59, 130, 246, 0.12)',
                        fill: true, tension: 0.4, pointRadius: 0, borderWidth: 2
                    }]
                },
                options: {
                    responsive: true, maintainAspectRatio: false,
                    plugins: { legend: { display: false }, tooltip: { enabled: false } },
                    scales: { x: { display: false }, y: { display: false } }
                }
            });
        }

        // Tab switching for the tabbed (narrow) modes — scoped to this card.
        window.prodxTab = function (btn) {
            var panel = btn.getAttribute('data-panel');
            card.querySelectorAll('.prodx__tabs .pa-tabs__item').forEach(function (t) {
                t.classList.toggle('pa-tabs__item--active', t === btn);
            });
            card.querySelectorAll('.prodx__panel').forEach(function (p) {
                p.classList.toggle('prodx__panel--active', p.getAttribute('data-panel') === panel);
            });
        };

        log('waiting — the revenue Chart.js instance is NOT built until the card reaches grid mode (~640px)');
        card.addEventListener('pc:breakpoint', function (e) {
            var w = Math.round(e.detail.width);
            log('mode → ' + e.detail.mode + ' (card ' + w + 'px)');
            if (e.detail.mode === 'grid') {
                if (!chart) {
                    chart = buildChart();
                    if (chart) { builds++; log('  ▶ built revenue chart instance #' + builds); }
                }
            } else if (chart) {
                chart.destroy();
                chart = null;
                log('  ✕ destroyed chart (not grid mode)');
            }
        });
    })();
</script>
