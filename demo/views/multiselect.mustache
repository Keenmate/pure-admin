<div class="pa-card">
  <div class="pa-card__header">
    <div class="pa-card__title">
        <h2 class="pa-card__title-text">Web Multiselect Component</h2>
    </div>
  </div>
  <div class="pa-card__body">
    <p>
      Advanced multiselect web component from <code>@keenmate/web-multiselect</code> (v2.0.0) with virtual
      scrolling, search, tree-of-options rendering, and a full-screen mobile overlay. Built as a
      framework-agnostic web component with excellent performance.
    </p>
    <p>
      <strong>Key Features:</strong> Virtual scrolling (handles 15,000+ options), full keyboard navigation,
      multiple display modes, <strong>tree / hierarchy mode</strong> with cascade checkboxes,
      <strong>phone full-screen overlay</strong>, RTL support, external/async search, and comprehensive
      theming via <code>--ms-*</code> / <code>--base-*</code> CSS variables — including
      <strong>swappable icons</strong> through the <code>--base-icon-*</code> tokens.
    </p>
  </div>
</div>

<!-- Basic Examples -->
<div class="pa-card">
  <div class="pa-card__header">
    <div class="pa-card__title">
        <h3 class="pa-card__title-text">Basic Multi-Select</h3>
    </div>
  </div>
  <div class="pa-card__body">
    <div class="pc-row">
      <div class="pc-col-100 pc-col-md-50">
        <div class="pa-form-group">
          <label>Programming Languages</label>
          <web-multiselect
            placeholder="Select languages..."
            value-member="value"
            display-value-member="label">
            <option value="js" selected>JavaScript</option>
            <option value="ts">TypeScript</option>
            <option value="python">Python</option>
            <option value="java">Java</option>
            <option value="csharp">C#</option>
            <option value="go">Go</option>
            <option value="rust">Rust</option>
            <option value="php">PHP</option>
            <option value="ruby">Ruby</option>
            <option value="swift">Swift</option>
          </web-multiselect>
        </div>
      </div>
      <div class="pc-col-100 pc-col-md-50">
        <div class="pa-form-group">
          <label>Single Select Mode</label>
          <web-multiselect placeholder="Select one option..." multiple="false" value-member="value" display-value-member="label">
            <option value="opt1">Option 1</option>
            <option value="opt2" selected>Option 2</option>
            <option value="opt3">Option 3</option>
            <option value="opt4">Option 4</option>
            <option value="opt5">Option 5</option>
          </web-multiselect>
        </div>
      </div>
    </div>
  </div>
</div>

<!-- Display Modes -->
<div class="pa-card">
  <div class="pa-card__header">
    <div class="pa-card__title">
        <h3 class="pa-card__title-text">Display Modes</h3>
    </div>
  </div>
  <div class="pa-card__body">
    <div class="pc-row">
      <div class="pc-col-100 pc-col-md-1-3">
        <div class="pa-form-group">
          <label>Pills Mode (Default)</label>
          <web-multiselect id="pills-demo" placeholder="Select items..." badges-display-mode="pills" value-member="value" display-value-member="label">
            <option value="1" selected>React</option>
            <option value="2" selected>Vue</option>
            <option value="3">Angular</option>
            <option value="4">Svelte</option>
            <option value="5">Solid</option>
          </web-multiselect>
          <small class="pa-form-text">Shows individual badges for each selection</small>
        </div>
      </div>
      <div class="pc-col-100 pc-col-md-1-3">
        <div class="pa-form-group">
          <label>Count Mode</label>
          <web-multiselect placeholder="Select items..." badges-display-mode="count" value-member="value" display-value-member="label">
            <option value="1" selected>Item 1</option>
            <option value="2" selected>Item 2</option>
            <option value="3">Item 3</option>
            <option value="4">Item 4</option>
            <option value="5">Item 5</option>
          </web-multiselect>
          <small class="pa-form-text">Shows "X selected" counter</small>
        </div>
      </div>
      <div class="pc-col-100 pc-col-md-1-3">
        <div class="pa-form-group">
          <label>Compact Mode</label>
          <web-multiselect placeholder="Select items..." badges-display-mode="compact" value-member="value" display-value-member="label">
            <option value="1" selected>First</option>
            <option value="2" selected>Second</option>
            <option value="3" selected>Third</option>
            <option value="4">Fourth</option>
            <option value="5">Fifth</option>
          </web-multiselect>
          <small class="pa-form-text">First item + "+X more" badge</small>
        </div>
      </div>
    </div>
    <div class="pc-row" style="margin-top: 1rem;">
      <div class="pc-col-100 pc-col-md-1-3">
        <div class="pa-form-group">
          <label>Partial Mode</label>
          <web-multiselect placeholder="Select items..." badges-display-mode="partial" value-member="value" display-value-member="label">
            <option value="1" selected>Alpha</option>
            <option value="2" selected>Beta</option>
            <option value="3" selected>Gamma</option>
            <option value="4">Delta</option>
            <option value="5">Epsilon</option>
          </web-multiselect>
          <small class="pa-form-text">Limited badges + "+X more"</small>
        </div>
      </div>
      <div class="pc-col-100 pc-col-md-1-3">
        <div class="pa-form-group">
          <label>None Mode</label>
          <web-multiselect
            id="none-mode"
            value-member="value"
            display-value-member="label"
            icon-member="icon"
            badges-display-mode="none"
            show-counter="true"
            search-placeholder="Select technologies...">
          </web-multiselect>
          <small class="pa-form-text">Minimal UI with no badges</small>
        </div>
      </div>
    </div>
  </div>
</div>

<!-- Search Features -->
<div class="pa-card">
  <div class="pa-card__header">
    <div class="pa-card__title">
        <h3 class="pa-card__title-text">Search &amp; Filter</h3>
    </div>
  </div>
  <div class="pa-card__body">
    <div class="pc-row">
      <div class="pc-col-100 pc-col-md-50">
        <div class="pa-form-group">
          <label>Search Mode: Filter</label>
          <web-multiselect placeholder="Search to filter..." search-enabled="true" search-mode="filter" value-member="value" display-value-member="label">
            <option value="apple">Apple</option>
            <option value="apricot">Apricot</option>
            <option value="banana">Banana</option>
            <option value="blueberry">Blueberry</option>
            <option value="cherry">Cherry</option>
            <option value="grape">Grape</option>
            <option value="kiwi">Kiwi</option>
            <option value="lemon">Lemon</option>
            <option value="mango">Mango</option>
            <option value="orange">Orange</option>
            <option value="peach">Peach</option>
            <option value="pear">Pear</option>
            <option value="strawberry">Strawberry</option>
            <option value="watermelon">Watermelon</option>
          </web-multiselect>
          <small class="pa-form-text">Hides non-matching options</small>
        </div>
      </div>
      <div class="pc-col-100 pc-col-md-50">
        <div class="pa-form-group">
          <label>Search Mode: Navigate</label>
          <web-multiselect placeholder="Search to navigate..." search-enabled="true" search-mode="navigate" value-member="value" display-value-member="label">
            <option value="argentina">Argentina</option>
            <option value="australia">Australia</option>
            <option value="brazil">Brazil</option>
            <option value="canada">Canada</option>
            <option value="china">China</option>
            <option value="france">France</option>
            <option value="germany">Germany</option>
            <option value="india">India</option>
            <option value="italy">Italy</option>
            <option value="japan">Japan</option>
            <option value="mexico">Mexico</option>
            <option value="spain">Spain</option>
            <option value="uk">United Kingdom</option>
            <option value="usa">United States</option>
          </web-multiselect>
          <small class="pa-form-text">Jumps to matches, keeps all visible</small>
        </div>
      </div>
    </div>
  </div>
</div>

<!-- ===================================================================== -->
<!-- Tree of Options (v2.0) -->
<!-- ===================================================================== -->
<div class="pa-card">
  <div class="pa-card__header">
    <div class="pa-card__title">
        <h3 class="pa-card__title-text"><svg xmlns="http://www.w3.org/2000/svg" width="1em" height="1em" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M8 5h13"/><path d="M13 12h8"/><path d="M13 19h8"/><path d="M3 10a2 2 0 0 0 2 2h3"/><path d="M3 5v12a2 2 0 0 0 2 2h3"/></svg> Tree of Options</h3>
    </div>
  </div>
  <div class="pa-card__body">
    <p>
      Give each option a materialized dot-path (<code>"1"</code>, <code>"1.1"</code>, <code>"1.1.1"</code>, …)
      and set <code>path-member</code>. Tree mode auto-enables: the component derives parent and depth from
      the path and renders the options depth-first, indented by level. The tree is always fully expanded
      (no chevron) — reach for <code>@keenmate/web-treeview</code> when you need expand/collapse.
    </p>

    <div class="pc-row" style="margin-top: 1rem;">
      <div class="pc-col-100 pc-col-md-50">
        <div class="pa-form-group">
          <label>Basic Tree — every node expanded</label>
          <web-multiselect id="tree-basic" multiple="true"></web-multiselect>
          <small class="pa-form-text">Branch + leaf nodes are all selectable, indented by depth</small>
        </div>
      </div>
      <div class="pc-col-100 pc-col-md-50">
        <div class="pa-form-group">
          <label>Ancestor-Preserving Search</label>
          <web-multiselect id="tree-search" multiple="true" search-enabled="true"></web-multiselect>
          <small class="pa-form-text">Open, type <code>kale</code> → Vegetables → Leafy → Kale (ancestors kept)</small>
        </div>
      </div>
    </div>

    <div class="pc-row">
      <div class="pc-col-100 pc-col-md-50">
        <div class="pa-form-group">
          <label>Selectable Leaves Only + Checkboxes</label>
          <web-multiselect id="tree-leaves" multiple="true" show-checkboxes="true"></web-multiselect>
          <small class="pa-form-text">Branches are pure structure — no checkbox, skipped by keyboard</small>
        </div>
      </div>
      <div class="pc-col-100 pc-col-md-50">
        <div class="pa-form-group">
          <label>Full Breadcrumb on Badges</label>
          <web-multiselect id="tree-fulltitle" multiple="true" show-badge-full-title="true"></web-multiselect>
          <small class="pa-form-text">Two items pre-selected — badges show <em>Fruit / Pome fruit / Apple</em></small>
        </div>
      </div>
    </div>

    <div class="pc-row">
      <div class="pc-col-100">
        <div class="pa-form-group">
          <label>Cascade Checkboxes — check a branch, check the whole subtree</label>
          <web-multiselect id="tree-cascade" multiple="true" show-checkboxes="true"
            checkbox-mode="cascade" actions-position="top"></web-multiselect>
          <small class="pa-form-text">
            Tristate (dash) on partial branches; Select All emits the rolled-up roots.
            Emitted <code>getValue()</code>: <code id="cascade-emitted" class="text-xs">[]</code>
          </small>
        </div>
      </div>
    </div>
  </div>
</div>

<!-- Real data — full ISCO-08 -->
<div class="pa-card">
  <div class="pa-card__header">
    <div class="pa-card__title">
        <h3 class="pa-card__title-text">Real Data — Full ISCO-08 Classification</h3>
    </div>
  </div>
  <div class="pa-card__body">
    <p>
      Everything at once on real data: the whole <strong>International Standard Classification of
      Occupations</strong> (ISCO-08) — 619 groups (10 major → 43 sub-major → 130 minor → 436 unit).
      The ISCO code <em>is</em> the hierarchy (<code>2211</code> → path <code>2.22.221.2211</code>), so it
      drops straight into <code>path-member</code>. Only the <strong>unit groups</strong> (leaves) are
      selectable. A <code>getSearchValueCallback</code> widens the filter to match the title, ISCO code,
      and full breadcrumb; virtual scroll keeps 600+ rows smooth.
    </p>
    <div class="pc-row" style="margin-top: 1rem;">
      <div class="pc-col-100">
        <div class="pa-form-group">
          <label>Search e.g. <code>welder</code>, <code>2211</code>, or <code>health</code> — then hover a badge</label>
          <web-multiselect id="tree-isco" multiple="true" search-enabled="true"
            enable-virtual-scroll="true" option-height="32"
            enable-badge-tooltips="true" enable-option-tooltips="true"
            badges-display-mode="partial" badges-max-visible="3"
            style="--ms-option-padding: 0.3rem 0.85rem; --ms-option-title-white-space: nowrap; --ms-option-title-overflow: hidden; --ms-option-title-text-overflow: ellipsis;"></web-multiselect>
          <small class="pa-form-text">
            Badge shows the leaf title only (prefixed <code>.. /</code>); full breadcrumb in the badge tooltip.
            Source: ISCO-08 (© International Labour Organization) — demo data.
          </small>
        </div>
      </div>
    </div>
  </div>
</div>

<!-- ===================================================================== -->
<!-- Mobile & Fullscreen (v2.0) -->
<!-- ===================================================================== -->
<div class="pa-card">
  <div class="pa-card__header">
    <div class="pa-card__title">
        <h3 class="pa-card__title-text"><svg xmlns="http://www.w3.org/2000/svg" width="1em" height="1em" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect width="14" height="20" x="5" y="2" rx="2" ry="2"/><path d="M12 18h.01"/></svg> Mobile &amp; Fullscreen</h3>
    </div>
  </div>
  <div class="pa-card__body">
    <p>
      On phones the dropdown opens as a <strong>full-screen overlay</strong> instead of a floating panel —
      a touch-friendly sheet with its own search header, big rows, and a close button. The
      <code>mobile-presentation</code> attribute (<code>auto</code> | <code>floating</code> |
      <code>fullscreen</code>) picks the presentation; <code>auto</code> uses the sheet only on phones.
      Because it's usually a phone-only look, each demo offers a <em>Preview as fullscreen</em> toggle so
      you can inspect the overlay on desktop.
    </p>

    <div class="pc-row" style="margin-top: 1rem;">
      <div class="pc-col-100 pc-col-md-1-3">
        <div class="pa-form-group">
          <label class="d-flex align-items-center gap-5" style="cursor:pointer;">
            <input type="checkbox" id="intro-fs-preview"> Preview as fullscreen (desktop)
          </label>
          <label>~50 grouped options</label>
          <web-multiselect id="mobile-intro" multiple="true" value-member="value"
            display-value-member="label" group-member="group" search-placeholder="Filter…"></web-multiselect>
          <small class="pa-form-text">On a phone this opens fullscreen automatically.</small>
        </div>
      </div>
      <div class="pc-col-100 pc-col-md-1-3">
        <div class="pa-form-group">
          <label class="d-flex align-items-center gap-5" style="cursor:pointer;">
            <input type="checkbox" id="kb-fs-preview"> Preview as fullscreen (desktop)
          </label>
          <label><code>fullscreen-autofocus="true"</code></label>
          <web-multiselect id="mobile-autofocus" multiple="true" value-member="value"
            display-value-member="label" fullscreen-autofocus="true" search-placeholder="Search…"></web-multiselect>
          <small class="pa-form-text">On a phone: search is focused, keyboard pops on open (vs. default keyboard-off).</small>
        </div>
      </div>
      <div class="pc-col-100 pc-col-md-1-3">
        <div class="pa-form-group">
          <label class="d-flex align-items-center gap-5" style="cursor:pointer;">
            <input type="checkbox" id="nav-fs-preview"> Preview as fullscreen (desktop)
          </label>
          <label><code>search-mode="navigate"</code> + match navigator</label>
          <web-multiselect id="mobile-navigate" multiple="true" search-mode="navigate"
            value-member="value" display-value-member="label" group-member="group"
            search-placeholder="Type to jump to matches…"></web-multiselect>
          <small class="pa-form-text">Fullscreen: type, then use the <code>N of M</code> ↑/↓ navigator to jump.</small>
        </div>
      </div>
    </div>
  </div>
</div>

<!-- Grouped Options -->
<div class="pa-card">
  <div class="pa-card__header">
    <div class="pa-card__title">
        <h3 class="pa-card__title-text">Grouped Options</h3>
    </div>
  </div>
  <div class="pa-card__body">
    <div class="pc-row">
      <div class="pc-col-100 pc-col-md-50">
        <div class="pa-form-group">
          <label>Technologies by Category</label>
          <web-multiselect
            id="grouped-demo"
            placeholder="Select technologies..."
            value-member="value"
            display-value-member="label"
            group-member="group">
          </web-multiselect>
          <small class="pa-form-text">Options grouped by Frontend, Backend, and Database categories</small>
        </div>
      </div>
      <div class="pc-col-100 pc-col-md-50">
        <div class="pa-form-group">
          <label>Disabled Options</label>
          <web-multiselect
            id="disabled-demo"
            placeholder="Some options disabled..."
            value-member="value"
            display-value-member="label"
            disabled-member="disabled">
          </web-multiselect>
          <small class="pa-form-text">Some options are disabled and cannot be selected</small>
        </div>
      </div>
    </div>
  </div>
</div>

<!-- Virtual Scrolling -->
<div class="pa-card">
  <div class="pa-card__header">
    <div class="pa-card__title">
        <h3 class="pa-card__title-text">Virtual Scrolling (Large Datasets)</h3>
    </div>
  </div>
  <div class="pa-card__body">
    <div class="pc-row">
      <div class="pc-col-100">
        <div class="pa-form-group">
          <label>1,000 Options with Virtual Scrolling</label>
          <web-multiselect id="virtual-scroll-demo" placeholder="Search 1,000 items..." search-enabled="true" value-member="value" display-value-member="label">
            <!-- Options will be added via JavaScript -->
          </web-multiselect>
          <small class="pa-form-text">
            Virtual scrolling handles massive datasets efficiently. Try searching to see instant filtering.
          </small>
        </div>
      </div>
    </div>
  </div>
</div>

<!-- RTL Support -->
<div class="pa-card">
  <div class="pa-card__header">
    <div class="pa-card__title">
        <h3 class="pa-card__title-text">RTL (Right-to-Left) Support</h3>
    </div>
  </div>
  <div class="pa-card__body">
    <div class="pc-row">
      <div class="pc-col-100 pc-col-md-50">
        <div class="pa-form-group">
          <label>Arabic (RTL)</label>
          <web-multiselect placeholder="بحث..." search-placeholder="بحث..." dir="rtl" badges-position="right" value-member="value" display-value-member="label">
            <option value="1">الخيار الأول</option>
            <option value="2">الخيار الثاني</option>
            <option value="3">الخيار الثالث</option>
            <option value="4">الخيار الرابع</option>
            <option value="5">الخيار الخامس</option>
          </web-multiselect>
          <small class="pa-form-text">Right-to-left layout for Arabic, Hebrew, Persian, etc.</small>
        </div>
      </div>
      <div class="pc-col-100 pc-col-md-50">
        <div class="pa-form-group">
          <label>Hebrew (RTL)</label>
          <web-multiselect placeholder="חיפוש..." search-placeholder="חיפוש..." dir="rtl" badges-position="right" value-member="value" display-value-member="label">
            <option value="1">אפשרות 1</option>
            <option value="2">אפשרות 2</option>
            <option value="3">אפשרות 3</option>
            <option value="4">אפשרות 4</option>
            <option value="5">אפשרות 5</option>
          </web-multiselect>
        </div>
      </div>
    </div>
  </div>
</div>

<!-- Custom Icons (v2.0) -->
<div class="pa-card">
  <div class="pa-card__header">
    <div class="pa-card__title">
        <h3 class="pa-card__title-text"><svg xmlns="http://www.w3.org/2000/svg" width="1em" height="1em" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M12 22a1 1 0 0 1 0-20 10 9 0 0 1 10 9 5 5 0 0 1-5 5h-2.25a1.75 1.75 0 0 0-1.4 2.8l.3.4a1.75 1.75 0 0 1-1.4 2.8z"/><circle cx="13.5" cy="6.5" r=".5" fill="currentColor"/><circle cx="17.5" cy="10.5" r=".5" fill="currentColor"/><circle cx="6.5" cy="12.5" r=".5" fill="currentColor"/><circle cx="8.5" cy="7.5" r=".5" fill="currentColor"/></svg> Custom Icons</h3>
    </div>
  </div>
  <div class="pa-card__body">
    <p>
      Every built-in glyph is a CSS variable, so re-skinning the picker is pure CSS — no slots, no JS.
      One <code>--base-icon-*</code> swap cascades to matching parts:
      <code>--base-icon-chevron</code> drives the toggle arrow <em>and</em> the fullscreen pager, while
      <code>--base-icon-close</code> covers <strong>every ✕</strong> (badge remove, count clear, popover close).
      Need finer control? <code>--base-icon-remove</code> (badge ✕), <code>--base-icon-clear</code>
      (count clear) and <code>--base-icon-search</code> override individually and win over
      <code>--base-icon-close</code>. Each is a full <code>url(...)</code> value — an inline SVG data-URI,
      an external file, whatever <code>background-image</code> accepts.
    </p>

    <!-- Scope the icon tokens to just these three demo instances. -->
    <style>
      /* A — one-line re-skin: soft rounded circle glyphs for the chevron + all ✕. */
      #icons-rounded {
        --base-icon-chevron: url("data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 24 24' fill='none' stroke='black' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><circle cx='12' cy='12' r='10'/><path d='m10 8 4 4-4 4'/></svg>");
        --base-icon-close: url("data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 24 24' fill='none' stroke='black' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><circle cx='12' cy='12' r='10'/><path d='m15 9-6 6'/><path d='m9 9 6 6'/></svg>");
      }

      /* B — granular: theme ONLY the badge remove ✕ (chunky filled square-x),
         leaving the chevron at its framework default. --base-icon-remove wins
         over --base-icon-close for the per-badge button. */
      #icons-granular {
        --base-icon-remove: url("data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 24 24' fill='black'><path d='M19 2h-14a3 3 0 0 0 -3 3v14a3 3 0 0 0 3 3h14a3 3 0 0 0 3 -3v-14a3 3 0 0 0 -3 -3zm-9.387 6.21l.094 .083l2.293 2.292l2.293 -2.292a1 1 0 0 1 1.497 1.32l-.083 .094l-2.292 2.293l2.292 2.293a1 1 0 0 1 -1.32 1.497l-.094 -.083l-2.293 -2.292l-2.293 2.292a1 1 0 0 1 -1.497 -1.32l.083 -.094l2.292 -2.293l-2.292 -2.293a1 1 0 0 1 1.32 -1.497z'/></svg>");
      }

      /* C — pre-oriented glyph + rotation opt-out: Material's dropdown indicator is a
         PRE-ORIENTED down-caret, so hand it a down glyph and tell the toggle NOT to
         rotate the source closed (0°) while still flipping it up on open (180°). */
      #icons-preoriented {
        --base-icon-chevron: url("data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 24 24' fill='black'><path d='M18 9c.852 0 1.297 .986 .783 1.623l-.076 .084l-6 6a1 1 0 0 1 -1.32 .083l-.094 -.083l-6 -6a1 1 0 0 1 1.32 -1.497l.094 .083l5.293 5.292l5.293 -5.292a1 1 0 0 1 .703 -.293z'/></svg>");
        --ms-toggle-rotate-closed: 0deg;
        --ms-toggle-rotate-open: 180deg;
      }
    </style>

    <div class="pc-row">
      <div class="pc-col-100 pc-col-md-1-3">
        <div class="pa-form-group">
          <label>One-line re-skin</label>
          <web-multiselect id="icons-rounded" badges-display-mode="pills"
            value-member="value" display-value-member="label">
            <option value="react" selected>React</option>
            <option value="vue" selected>Vue</option>
            <option value="svelte">Svelte</option>
            <option value="solid">Solid</option>
            <option value="angular">Angular</option>
          </web-multiselect>
          <small class="pa-form-text">
            <code>--base-icon-chevron</code> + <code>--base-icon-close</code> → rounded circle chevron and
            circle-✕ on every badge remove.
          </small>
        </div>
      </div>
      <div class="pc-col-100 pc-col-md-1-3">
        <div class="pa-form-group">
          <label>Granular — badge ✕ only</label>
          <web-multiselect id="icons-granular" badges-display-mode="pills"
            value-member="value" display-value-member="label">
            <option value="js" selected>JavaScript</option>
            <option value="ts" selected>TypeScript</option>
            <option value="py">Python</option>
            <option value="go">Go</option>
            <option value="rust">Rust</option>
          </web-multiselect>
          <small class="pa-form-text">
            Only <code>--base-icon-remove</code> is set — a filled square-✕ on badges, while the toggle
            chevron keeps the framework default.
          </small>
        </div>
      </div>
      <div class="pc-col-100 pc-col-md-1-3">
        <div class="pa-form-group">
          <label>Pre-oriented glyph + rotate opt-out</label>
          <web-multiselect id="icons-preoriented" multiple="false"
            value-member="value" display-value-member="label">
            <option value="low">Low</option>
            <option value="med" selected>Medium</option>
            <option value="high">High</option>
            <option value="crit">Critical</option>
          </web-multiselect>
          <small class="pa-form-text">
            A pre-oriented down-caret with <code>--ms-toggle-rotate-closed: 0deg</code> /
            <code>--ms-toggle-rotate-open: 180deg</code> — no rotation closed, flips up on open.
          </small>
        </div>
      </div>
    </div>
  </div>
</div>

<!-- Shared Icon Bridge — one --base-icon-* re-skins framework + web component together -->
<div class="pa-card">
  <div class="pa-card__header">
    <div class="pa-card__title">
        <h3 class="pa-card__title-text"><svg xmlns="http://www.w3.org/2000/svg" width="1em" height="1em" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M10 13a5 5 0 0 0 7.54.54l3-3a5 5 0 0 0-7.07-7.07l-1.72 1.71"/><path d="M14 11a5 5 0 0 0-7.54-.54l-3 3a5 5 0 0 0 7.07 7.07l1.71-1.71"/></svg> Shared Icon Bridge — Pure Admin ↔ Web Component</h3>
    </div>
  </div>
  <div class="pa-card__body">
    <p>
      The <code>--base-icon-*</code> tokens aren't private to the web component — they're a
      <strong>shared contract</strong>. Pure Admin's own controls read the exact same tokens:
      the custom <code>.pa-checkbox</code> paints its tick with <code>--base-icon-check</code> (as an
      SVG mask), which is the <em>identical</em> glyph <code>&lt;web-multiselect&gt;</code> masks for its
      selected rows. So overriding <code>--base-icon-check</code> <strong>once</strong> on a wrapper
      re-skins the framework checkbox and the web component in lockstep — no per-component theming, no JS.
    </p>

    <!-- Scope the shared token to this panel only. The star is deliberately
         un-checkmark-like so the "both changed together" effect is unmistakable. -->
    <style>
      #shared-icon-bridge {
        --base-icon-check: url("data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 24 24' fill='black'><path d='M12 2.5l2.9 5.88 6.49.94-4.7 4.58 1.11 6.46L12 17.9l-5.8 3.05 1.11-6.46-4.7-4.58 6.49-.94z'/></svg>");
      }
    </style>

    <div id="shared-icon-bridge" class="pc-row">
      <div class="pc-col-100 pc-col-md-50">
        <div class="pa-form-group">
          <label>Native Pure Admin <code>.pa-checkbox</code></label>
          <div class="d-flex flex-column gap-5" style="padding-top: 0.25rem;">
            <label class="pa-checkbox">
              <input type="checkbox" checked>
              <span class="pa-checkbox__box"></span>
              <span class="pa-checkbox__label">React</span>
            </label>
            <label class="pa-checkbox">
              <input type="checkbox" checked>
              <span class="pa-checkbox__box"></span>
              <span class="pa-checkbox__label">Svelte</span>
            </label>
            <label class="pa-checkbox">
              <input type="checkbox">
              <span class="pa-checkbox__box"></span>
              <span class="pa-checkbox__label">Angular</span>
            </label>
          </div>
          <small class="pa-form-text">Framework checkbox — tick masked from <code>--base-icon-check</code>.</small>
        </div>
      </div>
      <div class="pc-col-100 pc-col-md-50">
        <div class="pa-form-group">
          <label><code>&lt;web-multiselect&gt;</code> with <code>show-checkboxes</code></label>
          <web-multiselect id="shared-icon-ms" multiple="true" show-checkboxes="true"
            badges-display-mode="pills" value-member="value" display-value-member="label">
            <option value="react" selected>React</option>
            <option value="svelte" selected>Svelte</option>
            <option value="vue">Vue</option>
            <option value="solid">Solid</option>
            <option value="angular">Angular</option>
          </web-multiselect>
          <small class="pa-form-text">
            Open the dropdown — the checked rows carry the <strong>same star</strong>, from the same token.
          </small>
        </div>
      </div>
    </div>

    <div class="pa-alert pa-alert--info mt-4">
      <small>
        This is the whole point of the <code>--base-*</code> bridge: it lets shadow-DOM web components and
        the SCSS-compiled framework share one theming surface. Pure Admin emits the defaults at
        <code>:root</code> via <code>_base-css-variables.scss</code>, so out of the box the picker already
        matches the framework's glyphs — this panel just proves the swap propagates to both at once.
      </small>
    </div>
  </div>
</div>

<!-- Programmatic Control -->
<div class="pa-card">
  <div class="pa-card__header">
    <div class="pa-card__title">
        <h3 class="pa-card__title-text">Programmatic Control</h3>
    </div>
  </div>
  <div class="pa-card__body">
    <div class="pc-row">
      <div class="pc-col-100 pc-col-md-2-3">
        <div class="pa-form-group">
          <label>Controlled via JavaScript</label>
          <web-multiselect id="programmatic-demo" placeholder="Use buttons to control..." value-member="value" display-value-member="label">
            <option value="red">Red</option>
            <option value="green">Green</option>
            <option value="blue">Blue</option>
            <option value="yellow">Yellow</option>
            <option value="purple">Purple</option>
            <option value="orange">Orange</option>
          </web-multiselect>
        </div>
      </div>
      <div class="pc-col-100 pc-col-md-1-3">
        <div class="pa-form-group">
          <label>&nbsp;</label>
          <div class="pa-btn-group d-flex flex-column gap-5">
            <button class="pa-btn pa-btn--sm pa-btn--primary" onclick="selectColors(['red', 'blue'])">
              Select Red &amp; Blue
            </button>
            <button class="pa-btn pa-btn--sm pa-btn--secondary" onclick="getSelectedColors()">
              Get Selected
            </button>
            <button class="pa-btn pa-btn--sm pa-btn--danger" onclick="clearColors()">
              Clear All
            </button>
          </div>
        </div>
      </div>
    </div>
    <div class="pc-row">
      <div class="pc-col-100">
        <div id="selected-output" class="text-sm mt-2" style="padding: 0.5rem; background: var(--base-primary-bg); border-radius: 4px; font-family: monospace;"></div>
      </div>
    </div>
  </div>
</div>

<!-- Events -->
<div class="pa-card">
  <div class="pa-card__header">
    <div class="pa-card__title">
        <h3 class="pa-card__title-text">Events &amp; Callbacks</h3>
    </div>
  </div>
  <div class="pa-card__body">
    <div class="pc-row">
      <div class="pc-col-100 pc-col-md-50">
        <div class="pa-form-group">
          <label>Monitor Changes</label>
          <web-multiselect id="events-demo" placeholder="Select to see events..." value-member="value" display-value-member="label">
            <option value="event1">Event Option 1</option>
            <option value="event2">Event Option 2</option>
            <option value="event3">Event Option 3</option>
            <option value="event4">Event Option 4</option>
            <option value="event5">Event Option 5</option>
          </web-multiselect>
        </div>
      </div>
      <div class="pc-col-100 pc-col-md-50">
        <div class="pa-form-group">
          <label>Event Log</label>
          <div id="event-log" class="text-xs hr-15 overflow-y-auto" style="padding: 0.5rem; background: var(--base-subtle-bg); border-radius: 4px; font-family: monospace;"></div>
        </div>
      </div>
    </div>
  </div>
</div>

<!-- Documentation Links -->
<div class="pa-card">
  <div class="pa-card__header">
    <div class="pa-card__title">
        <h3 class="pa-card__title-text">Resources</h3>
    </div>
  </div>
  <div class="pa-card__body">
    <ul>
      <li><strong>npm Package:</strong> <code>@keenmate/web-multiselect</code> (v2.0.0)</li>
      <li><strong>Repository:</strong> <a href="https://github.com/keenmate/web-multiselect" target="_blank" class="pa-link">github.com/keenmate/web-multiselect</a></li>
      <li><strong>Documentation:</strong> See package README and AI instructions in <code>../../web-multiselect/ai/</code></li>
      <li><strong>CSS Variables:</strong> Uses <code>--ms-*</code> prefix (component) and <code>--base-*</code> prefix (theme integration)</li>
      <li><strong>Custom Icons:</strong> <code>--base-icon-chevron</code> (toggle + pager), <code>--base-icon-close</code> (all&nbsp;✕), plus granular <code>--base-icon-remove</code> / <code>--base-icon-clear</code> / <code>--base-icon-search</code>; opt out of chevron rotation with <code>--ms-toggle-rotate-closed/open</code></li>
      <li><strong>Theming:</strong> Pure Admin provides <code>--base-*</code> variables via <code>src/scss/_base-css-variables.scss</code></li>
    </ul>
  </div>
</div>

<script>
// Virtual scrolling demo - add 1,000 options
(function() {
  const virtualDemo = document.getElementById('virtual-scroll-demo');
  if (virtualDemo) {
    for (let i = 1; i <= 1000; i++) {
      const option = document.createElement('option');
      option.value = `item${i}`;
      option.textContent = `Item ${i} - Option ${i}`;
      virtualDemo.appendChild(option);
    }
  }
})();

// Tree demo data — each option carries a materialized dot-path; parent/level are
// derived from it. Lifted from the web-multiselect examples-tree.html demo.
const treeCategories = [
  { value: 'fruit',     label: 'Fruit',       path: '1' },
  { value: 'pome',      label: 'Pome fruit',  path: '1.1' },
  { value: 'apple',     label: 'Apple',       path: '1.1.1' },
  { value: 'pear',      label: 'Pear',        path: '1.1.2' },
  { value: 'quince',    label: 'Quince',      path: '1.1.3' },
  { value: 'stone',     label: 'Stone fruit', path: '1.2' },
  { value: 'peach',     label: 'Peach',       path: '1.2.1' },
  { value: 'plum',      label: 'Plum',        path: '1.2.2' },
  { value: 'cherry',    label: 'Cherry',      path: '1.2.3' },
  { value: 'apricot',   label: 'Apricot',     path: '1.2.4' },
  { value: 'berries',   label: 'Berries',     path: '1.3' },
  { value: 'straw',     label: 'Strawberry',  path: '1.3.1' },
  { value: 'blue',      label: 'Blueberry',   path: '1.3.2' },
  { value: 'rasp',      label: 'Raspberry',   path: '1.3.3' },
  { value: 'citrus',    label: 'Citrus',      path: '1.4' },
  { value: 'orange',    label: 'Orange',      path: '1.4.1' },
  { value: 'lemon',     label: 'Lemon',       path: '1.4.2' },
  { value: 'lime',      label: 'Lime',        path: '1.4.3' },
  { value: 'veg',       label: 'Vegetables',  path: '2' },
  { value: 'root',      label: 'Root',        path: '2.1' },
  { value: 'carrot',    label: 'Carrot',      path: '2.1.1' },
  { value: 'beet',      label: 'Beetroot',    path: '2.1.2' },
  { value: 'radish',    label: 'Radish',      path: '2.1.3' },
  { value: 'leafy',     label: 'Leafy',       path: '2.2' },
  { value: 'kale',      label: 'Kale',        path: '2.2.1' },
  { value: 'spinach',   label: 'Spinach',     path: '2.2.2' },
  { value: 'lettuce',   label: 'Lettuce',     path: '2.2.3' },
  { value: 'chard',     label: 'Chard',       path: '2.2.4' },
  { value: 'brassica',  label: 'Brassica',    path: '2.3' },
  { value: 'broccoli',  label: 'Broccoli',    path: '2.3.1' },
  { value: 'caulif',    label: 'Cauliflower', path: '2.3.2' },
  { value: 'cabbage',   label: 'Cabbage',     path: '2.3.3' },
  { value: 'dairy',     label: 'Dairy',       path: '3' },
  { value: 'cheese',    label: 'Cheese',      path: '3.1' },
  { value: 'cheddar',   label: 'Cheddar',     path: '3.1.1' },
  { value: 'brie',      label: 'Brie',        path: '3.1.2' },
  { value: 'gouda',     label: 'Gouda',       path: '3.1.3' },
  { value: 'milk',      label: 'Milk',        path: '3.2' },
  { value: 'milk-w',    label: 'Whole milk',  path: '3.2.1' },
  { value: 'milk-s',    label: 'Skim milk',   path: '3.2.2' }
];

// A "full title" ships WITH the data — build a breadcrumb by joining ancestor labels.
const labelByPath = new Map(treeCategories.map(c => [c.path, c.label]));
const treeWithFullTitles = treeCategories.map(c => {
  const segs = c.path.split('.');
  const crumb = segs
    .map((_, i) => labelByPath.get(segs.slice(0, i + 1).join('.')))
    .filter(Boolean)
    .join(' / ');
  return { ...c, fullTitle: crumb };
});

// Grouped data for the mobile demos (~50 options, 8 groups).
const mobileToOptions = (catalog) =>
  Object.entries(catalog).flatMap(([group, labels], gi) =>
    labels.map((label, i) => ({ value: `${gi}-${i}`, label, group })));
const mobileOptions = mobileToOptions({
  'Compute': ['Virtual Machines', 'Kubernetes', 'Serverless Functions', 'Container Registry', 'Batch', 'Auto Scaling', 'Load Balancer'],
  'Storage': ['Object Storage', 'Block Storage', 'File Storage', 'Cold Archive', 'Backup Vault', 'CDN'],
  'Networking': ['Virtual Network', 'DNS', 'VPN Gateway', 'Firewall', 'NAT Gateway', 'Private Link', 'Traffic Manager'],
  'Databases': ['SQL Database', 'NoSQL Store', 'In-Memory Cache', 'Data Warehouse', 'Graph Database', 'Time Series DB', 'Search Engine'],
  'Security': ['Identity & Access', 'Secrets Manager', 'Key Vault', 'Certificate Manager', 'Web App Firewall', 'DDoS Protection', 'Audit Logs'],
  'Analytics': ['Data Lake', 'Stream Processing', 'BI Dashboards', 'ETL Pipelines', 'Event Bus', 'Metrics'],
  'Developer Tools': ['CI/CD Pipelines', 'Source Repos', 'Artifact Registry', 'API Gateway', 'Feature Flags', 'Monitoring'],
  'Messaging': ['Message Queue', 'Pub/Sub', 'Transactional Email', 'SMS', 'Push Notifications']
});

customElements.whenDefined('web-multiselect').then(() => {
  // ---- None Mode demo ----
  const noneMode = document.getElementById('none-mode');
  if (noneMode) {
    customElements.upgrade(noneMode);
    noneMode.options = [
      { value: 'js', label: 'JavaScript', icon: '🟨' },
      { value: 'ts', label: 'TypeScript', icon: '🔷' },
      { value: 'py', label: 'Python', icon: '🐍' },
      { value: 'rust', label: 'Rust', icon: '🦀' },
      { value: 'go', label: 'Go', icon: '🐹' },
      { value: 'java', label: 'Java', icon: '☕' },
      { value: 'csharp', label: 'C#', icon: '🟣' },
      { value: 'cpp', label: 'C++', icon: '🔵' }
    ];
    noneMode.value = ['js', 'ts'];
  }

  // ---- Grouped options demo ----
  const groupedDemo = document.getElementById('grouped-demo');
  if (groupedDemo) {
    customElements.upgrade(groupedDemo);
    groupedDemo.options = [
      { value: 'react', label: 'React', group: 'Frontend' },
      { value: 'vue', label: 'Vue', group: 'Frontend' },
      { value: 'angular', label: 'Angular', group: 'Frontend' },
      { value: 'svelte', label: 'Svelte', group: 'Frontend' },
      { value: 'nodejs', label: 'Node.js', group: 'Backend' },
      { value: 'django', label: 'Django', group: 'Backend' },
      { value: 'rails', label: 'Ruby on Rails', group: 'Backend' },
      { value: 'laravel', label: 'Laravel', group: 'Backend' },
      { value: 'postgresql', label: 'PostgreSQL', group: 'Database' },
      { value: 'mysql', label: 'MySQL', group: 'Database' },
      { value: 'mongodb', label: 'MongoDB', group: 'Database' },
      { value: 'redis', label: 'Redis', group: 'Database' }
    ];
  }

  // ---- Disabled options demo ----
  const disabledDemo = document.getElementById('disabled-demo');
  if (disabledDemo) {
    customElements.upgrade(disabledDemo);
    disabledDemo.options = [
      { value: 'enabled1', label: 'Available Option 1' },
      { value: 'disabled1', label: 'Unavailable Option 1', disabled: true },
      { value: 'enabled2', label: 'Available Option 2' },
      { value: 'disabled2', label: 'Unavailable Option 2', disabled: true },
      { value: 'enabled3', label: 'Available Option 3' },
      { value: 'disabled3', label: 'Unavailable Option 3', disabled: true }
    ];
  }

  // ---- Tree demos (dot-path) ----
  for (const id of ['tree-basic', 'tree-search']) {
    const el = document.getElementById(id);
    if (!el) continue;
    el.valueMember = 'value';
    el.displayValueMember = 'label';
    el.pathMember = 'path';
    el.options = treeCategories;
  }

  // Leaves-only selectability: branches are pure structure.
  const treeLeaves = document.getElementById('tree-leaves');
  if (treeLeaves) {
    treeLeaves.valueMember = 'value';
    treeLeaves.displayValueMember = 'label';
    treeLeaves.pathMember = 'path';
    treeLeaves.getIsSelectableCallback = (node) => !node.hasChildren;
    treeLeaves.options = treeCategories;
  }

  // Full breadcrumb on badges (two items pre-selected).
  const treeFullTitle = document.getElementById('tree-fulltitle');
  if (treeFullTitle) {
    treeFullTitle.valueMember = 'value';
    treeFullTitle.displayValueMember = 'label';
    treeFullTitle.pathMember = 'path';
    treeFullTitle.fullTitleMember = 'fullTitle';
    treeFullTitle.options = treeWithFullTitles;
    treeFullTitle.setSelected(['apple', 'kale']);
  }

  // Cascade checkboxes with select-all/clear-all + custom action.
  const treeCascade = document.getElementById('tree-cascade');
  if (treeCascade) {
    treeCascade.valueMember = 'value';
    treeCascade.displayValueMember = 'label';
    treeCascade.pathMember = 'path';
    treeCascade.options = treeCategories;
    treeCascade.actionButtons = [
      { action: 'select-all', text: 'Select All' },
      { action: 'clear-all', text: 'Clear All' },
      { action: 'custom', text: 'All fruit', onClick: (ms) => ms.setSelected(['fruit'], { notify: true }) }
    ];
    const emitted = document.getElementById('cascade-emitted');
    const showEmitted = () => { if (emitted) emitted.textContent = JSON.stringify(treeCascade.getValue()); };
    treeCascade.addEventListener('change', showEmitted);
  }

  // Real data — the full ISCO-08 occupation classification (619 groups).
  fetch('/assets/data/isco08.json')
    .then(r => r.json())
    .then(isco => {
      const el = document.getElementById('tree-isco');
      if (!el) return;
      el.valueMember = 'value';
      el.displayValueMember = 'label';
      el.pathMember = 'path';
      el.isSelectableMember = 'selectable';
      // Widen the built-in filter to also match ISCO code + full breadcrumb.
      el.getSearchValueCallback = (item) => `${item.label} ${item.value} ${item.fullTitle}`;
      // Badge = last segment only, with a ".. /" hint when ancestors exist.
      el.getBadgeDisplayCallback = (item) => {
        const segs = String(item.fullTitle || item.label).split(' / ');
        const last = segs[segs.length - 1];
        return segs.length > 1 ? `.. / ${last}` : last;
      };
      el.getBadgeTooltipCallback = (item) => item.fullTitle || item.label;
      el.options = isco;
    })
    .catch(err => console.error('ISCO-08 tree demo failed to load:', err));

  // ---- Mobile demos ----
  const mobileIntro = document.getElementById('mobile-intro');
  const mobileAutofocus = document.getElementById('mobile-autofocus');
  const mobileNavigate = document.getElementById('mobile-navigate');
  [mobileIntro, mobileAutofocus, mobileNavigate].forEach((el) => { if (el) el.options = mobileOptions; });

  // "Preview as fullscreen" checkbox flips mobile-presentation between
  // 'fullscreen' (forced overlay on desktop) and 'auto' (floating here).
  const wirePreview = (checkboxId, ...els) => {
    const cb = document.getElementById(checkboxId);
    if (!cb) return;
    cb.addEventListener('change', (e) => {
      const mode = e.target.checked ? 'fullscreen' : 'auto';
      els.forEach((el) => { if (el) el.setAttribute('mobile-presentation', mode); });
    });
  };
  wirePreview('intro-fs-preview', mobileIntro);
  wirePreview('kb-fs-preview', mobileAutofocus);
  wirePreview('nav-fs-preview', mobileNavigate);
});

// Programmatic control functions
function selectColors(values) {
  const select = document.getElementById('programmatic-demo');
  if (select && select.setSelected) {
    select.setSelected(values);
  }
}

function getSelectedColors() {
  const select = document.getElementById('programmatic-demo');
  const output = document.getElementById('selected-output');
  if (select && output) {
    const selected = select.getSelected ? select.getSelected() : [];
    const values = select.getValue ? select.getValue() : [];
    output.innerHTML = `
      <strong>Selected Options:</strong><br>
      Values: ${JSON.stringify(values)}<br>
      Objects: ${JSON.stringify(selected, null, 2)}
    `;
  }
}

function clearColors() {
  const select = document.getElementById('programmatic-demo');
  if (select && select.setSelected) {
    select.setSelected([]);
  }
}

// Events demo
(function() {
  const eventsDemo = document.getElementById('events-demo');
  const eventLog = document.getElementById('event-log');

  if (eventsDemo && eventLog) {
    eventsDemo.addEventListener('change', (e) => {
      const timestamp = new Date().toLocaleTimeString();
      const values = e.detail.selectedValues || [];
      const logEntry = document.createElement('div');
      logEntry.style.marginBottom = '0.25rem';
      logEntry.textContent = `[${timestamp}] Changed: ${JSON.stringify(values)}`;
      eventLog.insertBefore(logEntry, eventLog.firstChild);

      // Keep only last 10 entries
      while (eventLog.children.length > 10) {
        eventLog.removeChild(eventLog.lastChild);
      }
    });
  }
})();
</script>
</content>
</invoke>
