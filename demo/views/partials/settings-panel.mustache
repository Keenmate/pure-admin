<!-- Floating Settings Panel -->
<div class="pa-settings-panel" id="settingsPanel">
    <button class="pa-settings-panel__toggle" id="settingsToggle" title="Settings" aria-label="Settings">
        <span class="pa-icon pa-icon--settings" aria-hidden="true"></span>
    </button>

    <div class="pa-settings-panel__content">
        <h3 class="pa-settings-panel__title">Settings</h3>

        <!-- Theme Selector (populated dynamically from theme manifests) -->
        <div class="pa-settings-panel__section">
            <label class="pa-settings-panel__label">Theme</label>
            <select class="pa-settings-panel__select" id="themeSelector">
                <!-- Options populated dynamically from /api/themes/manifests -->
                <option value="">Loading...</option>
            </select>
        </div>

        <!-- Theme Mode (populated dynamically based on theme manifest) -->
        <div class="pa-settings-panel__section" id="themeModeSection" style="display: none;">
            <label class="pa-settings-panel__label">Mode</label>
            <select class="pa-settings-panel__select" id="themeModeSelector">
                <!-- Options populated dynamically from theme manifest -->
            </select>
        </div>

        <!-- Color Variant (populated dynamically based on theme manifest) -->
        <div class="pa-settings-panel__section" id="colorVariantSection" style="display: none;">
            <label class="pa-settings-panel__label">Color Variant</label>
            <select class="pa-settings-panel__select" id="colorVariantSelector">
                <!-- Options populated dynamically from theme manifest -->
            </select>
        </div>

        <!-- Layout Width -->
        <div class="pa-settings-panel__section">
            <label class="pa-settings-panel__label">Layout Width</label>
            <select class="pa-settings-panel__select" id="containerWidthSelector">
                <option value="fluid">Fluid (Full Width)</option>
                <option value="sm">Small (768px)</option>
                <option value="md">Medium (1024px)</option>
                <option value="lg">Large (1280px)</option>
                <option value="xl">Extra Large (1600px)</option>
                <option value="2xl">2X Large (1920px)</option>
            </select>
        </div>

        <!-- Search Box position — previews the search placements -->
        <div class="pa-settings-panel__section">
            <label class="pa-settings-panel__label">Search Box</label>
            <select class="pa-settings-panel__select" id="searchPositionSelector">
                <option value="">Off</option>
                <option value="navbar-inline">Navbar — inline search (A)</option>
                <option value="navbar-compact">Navbar — compact, opens palette (B)</option>
                {{^simpleLayout}}<option value="sidebar">Sidebar — opens palette (C)</option>{{/simpleLayout}}
                <option value="navbar-typeahead">Navbar — type &amp; go → /search (D)</option>
                {{^simpleLayout}}<option value="sidebar-typeahead">Sidebar — type &amp; go → /search (E)</option>{{/simpleLayout}}
            </select>
        </div>

        <!-- Command Palette width — sets the --sm/--lg/--xl preset on the palette
             (Ctrl+K to preview). Default (no modifier) is 608px. -->
        <div class="pa-settings-panel__section">
            <label class="pa-settings-panel__label">Command Palette</label>
            <select class="pa-settings-panel__select" id="commandPaletteSizeSelector">
                <option value="">Default (608px)</option>
                <option value="sm">Small (480px)</option>
                <option value="lg">Large (768px)</option>
                <option value="xl">Extra Large (896px)</option>
            </select>
            <small class="pa-settings-panel__hint">Press Ctrl+K to preview the palette.</small>
        </div>

        <!-- Sidebar sections: kept in the DOM (settings-panel.js references these
             selectors unguarded), but hidden in the sidebar-less simple layout. -->
        <!-- Sidebar Mode -->
        <div class="pa-settings-panel__section" {{#simpleLayout}}style="display:none"{{/simpleLayout}}>
            <label class="pa-settings-panel__label">Sidebar Mode</label>
            <select class="pa-settings-panel__select" id="sidebarModeSelector">
                <option value="">Scrolls with Content</option>
                <option value="sticky">Fixed Position</option>
            </select>
        </div>

        <!-- Sidebar Behavior -->
        <div class="pa-settings-panel__section" {{#simpleLayout}}style="display:none"{{/simpleLayout}}>
            <label class="pa-settings-panel__label">Sidebar Behavior</label>
            <select class="pa-settings-panel__select" id="sidebarBehaviorSelector">
                <option value="hide">Hide Completely</option>
                <option value="icon-collapse">Show Icons Only</option>
            </select>
        </div>

        <!-- Sidebar Options -->
        <div class="pa-settings-panel__section" {{#simpleLayout}}style="display:none"{{/simpleLayout}}>
            <label class="pa-settings-panel__label">Sidebar</label>
            <div class="pa-settings-panel__checkbox-group">
                <label class="pa-checkbox">
                    <input type="checkbox" id="sidebarResizable">
                    <span class="pa-checkbox__box"></span>
                    <span class="pa-checkbox__label">Resizable</span>
                </label>
            </div>
        </div>

        <!-- Display Options -->
        <div class="pa-settings-panel__section">
            <label class="pa-settings-panel__label">Display</label>
            <div class="pa-settings-panel__checkbox-group">
                <label class="pa-checkbox">
                    <input type="checkbox" id="compactMode">
                    <span class="pa-checkbox__box"></span>
                    <span class="pa-checkbox__label">Compact Mode</span>
                </label>
                <label class="pa-checkbox">
                    <input type="checkbox" id="rtlMode">
                    <span class="pa-checkbox__box"></span>
                    <span class="pa-checkbox__label">RTL Mode</span>
                </label>
            </div>
        </div>

        <!-- Profile Panel -->
        <div class="pa-settings-panel__section">
            <label class="pa-settings-panel__label">Profile Panel</label>
            <div class="pa-settings-panel__checkbox-group">
                <label class="pa-checkbox">
                    <input type="checkbox" id="profileNoAvatar">
                    <span class="pa-checkbox__box"></span>
                    <span class="pa-checkbox__label">Hide Avatar</span>
                </label>
                <label class="pa-checkbox">
                    <input type="checkbox" id="profileIconOnlyTabs">
                    <span class="pa-checkbox__box"></span>
                    <span class="pa-checkbox__label">Icon-Only Tabs</span>
                </label>
            </div>
        </div>

        <!-- Font Size -->
        <div class="pa-settings-panel__section">
            <label class="pa-settings-panel__label">Font Size</label>
            <select class="pa-settings-panel__select" id="fontSizeSelector">
                <option value="small">Small (14px)</option>
                <option value="default">Default (16px)</option>
                <option value="large">Large (18px)</option>
                <option value="xlarge">Extra Large (20px)</option>
            </select>
            <small class="pa-settings-panel__hint">Body text size. All elements scale proportionally.</small>
        </div>

        <!-- Font Family -->
        <div class="pa-settings-panel__section">
            <label class="pa-settings-panel__label">Font Family</label>
            <select class="pa-settings-panel__select" id="fontFamilySelector">
                <option value="default">Theme Default</option>
                <option value="serif">Serif</option>
                <option value="mono">Monospace</option>
                <option value="cuprum">Cuprum</option>
                <option value="fira-sans-condensed">Fira Sans Condensed</option>
                <option value="manrope">Manrope</option>
                <option value="martel">Martel</option>
                <option value="maven-pro">Maven Pro</option>
                <option value="monda">Monda</option>
                <option value="play">Play</option>
                <option value="signika">Signika</option>
                <option value="yanone-kaffeesatz">Yanone Kaffeesatz</option>
            </select>
        </div>

        <!-- Reset Button -->
        <div class="pa-settings-panel__section">
            <button class="pa-btn pa-btn--secondary pa-btn--block" id="resetSettings">
                Reset to Defaults
            </button>
        </div>
    </div>
</div>
