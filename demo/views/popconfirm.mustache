<!-- Popconfirm Component Examples -->

<div class="pa-card">
    <div class="pa-card__header">
        <div class="pa-card__title">
            <h3 class="pa-card__title-text">Popconfirm Component</h3>
        </div>
        <p>Small confirmation dialogs anchored to trigger buttons - perfect for delete confirmations and quick decisions</p>
    </div>
    <div class="pa-card__body">
        <div class="pc-row">
            <div class="pc-col-100 pc-col-md-50">
                <h4>Basic Popconfirms</h4>
                <div class="minhr-12">
                    <div class="pa-btn-group">
                        <button
                            class="pa-btn pa-btn--danger"
                            onclick="togglePopconfirm(event, 'popconfirm-delete')"
                        >
                            Delete Item
                        </button>

                        <button
                            class="pa-btn pa-btn--warning"
                            onclick="togglePopconfirm(event, 'popconfirm-warning')"
                        >
                            Archive Item
                        </button>

                        <button
                            class="pa-btn pa-btn--secondary"
                            onclick="togglePopconfirm(event, 'popconfirm-info')"
                        >
                            Reset Settings
                        </button>
                    </div>
                </div>
            </div>

            <div class="pc-col-100 pc-col-md-50">
                <h4>Compact Variant</h4>
                <div class="minhr-12">
                    <div class="pa-btn-group">
                        <button
                            class="pa-btn pa-btn--xs pa-btn--danger"
                            onclick="togglePopconfirm(event, 'popconfirm-compact')"
                            aria-label="Delete"
                        >
                            <span class="pa-icon pa-icon--delete" aria-hidden="true"></span>
                        </button>

                        <button
                            class="pa-btn pa-btn--xs pa-btn--outline-danger"
                            onclick="togglePopconfirm(event, 'popconfirm-compact-2')"
                        >
                            Remove
                        </button>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<!-- Table Example -->
<div class="pa-table-card">
    <div class="pa-table-card__header">
        <h3>Table with Popconfirms</h3>
        <p>Common use case: delete confirmations in data tables</p>
    </div>
    <div class="pa-table-card__body pa-table-card__body--scrollable">
        <table class="pa-table pa-table--striped">
            <thead>
                <tr>
                    <th class="col-auto">Actions</th>
                    <th>ID</th>
                    <th>Name</th>
                    <th>Status</th>
                </tr>
            </thead>
            <tbody>
                <tr>
                    <td class="col-auto">
                        <div class="pa-btn-group">
                            <button class="pa-btn pa-btn--xs pa-btn--primary">Edit</button>
                            <button
                                class="pa-btn pa-btn--xs pa-btn--danger"
                                onclick="togglePopconfirm(event, 'popconfirm-table-1')"
                            >
                                Delete
                            </button>
                        </div>
                    </td>
                    <td>1</td>
                    <td>John Doe</td>
                    <td><span class="pa-badge pa-badge--success">Active</span></td>
                </tr>
                <tr>
                    <td class="col-auto">
                        <div class="pa-btn-group">
                            <button class="pa-btn pa-btn--xs pa-btn--primary">Edit</button>
                            <button
                                class="pa-btn pa-btn--xs pa-btn--danger"
                                onclick="togglePopconfirm(event, 'popconfirm-table-2')"
                            >
                                Delete
                            </button>
                        </div>
                    </td>
                    <td>2</td>
                    <td>Jane Smith</td>
                    <td><span class="pa-badge pa-badge--warning">Pending</span></td>
                </tr>
                <tr>
                    <td class="col-auto">
                        <div class="pa-btn-group">
                            <button class="pa-btn pa-btn--xs pa-btn--primary">Edit</button>
                            <button
                                class="pa-btn pa-btn--xs pa-btn--danger"
                                onclick="togglePopconfirm(event, 'popconfirm-table-3')"
                            >
                                Delete
                            </button>
                        </div>
                    </td>
                    <td>3</td>
                    <td>Bob Johnson</td>
                    <td><span class="pa-badge pa-badge--secondary">Inactive</span></td>
                </tr>
            </tbody>
        </table>
    </div>
</div>

<!-- Popconfirm Definitions -->

<!-- Delete Confirmation -->
<div id="popconfirm-delete" class="pa-popconfirm pa-popconfirm--bottom">
    <div class="pa-popconfirm__arrow"></div>
    <div class="pa-popconfirm__content">
        <div class="pa-popconfirm__message pa-popconfirm__icon pa-popconfirm__icon--danger">
            <p>Are you sure you want to delete this item? This action cannot be undone.</p>
        </div>
        <div class="pa-popconfirm__actions">
            <button class="pa-btn pa-btn--secondary" onclick="closePopconfirm('popconfirm-delete')">Cancel</button>
            <button class="pa-btn pa-btn--danger" onclick="confirmDelete('popconfirm-delete')">Delete</button>
        </div>
    </div>
</div>

<!-- Warning Confirmation -->
<div id="popconfirm-warning" class="pa-popconfirm pa-popconfirm--bottom">
    <div class="pa-popconfirm__arrow"></div>
    <div class="pa-popconfirm__content">
        <div class="pa-popconfirm__message pa-popconfirm__icon pa-popconfirm__icon--warning">
            <p>Archive this item? It will be moved to the archive folder.</p>
        </div>
        <div class="pa-popconfirm__actions">
            <button class="pa-btn pa-btn--secondary" onclick="closePopconfirm('popconfirm-warning')">Cancel</button>
            <button class="pa-btn pa-btn--warning" onclick="confirmDelete('popconfirm-warning')">Archive</button>
        </div>
    </div>
</div>

<!-- Info Confirmation -->
<div id="popconfirm-info" class="pa-popconfirm pa-popconfirm--bottom">
    <div class="pa-popconfirm__arrow"></div>
    <div class="pa-popconfirm__content">
        <div class="pa-popconfirm__message pa-popconfirm__icon pa-popconfirm__icon--info">
            <p>Reset all settings to default values?</p>
        </div>
        <div class="pa-popconfirm__actions">
            <button class="pa-btn pa-btn--secondary" onclick="closePopconfirm('popconfirm-info')">Cancel</button>
            <button class="pa-btn pa-btn--primary" onclick="confirmDelete('popconfirm-info')">Reset</button>
        </div>
    </div>
</div>

<!-- Compact Popconfirm -->
<div id="popconfirm-compact" class="pa-popconfirm pa-popconfirm--bottom pa-popconfirm--compact">
    <div class="pa-popconfirm__arrow"></div>
    <div class="pa-popconfirm__content">
        <div class="pa-popconfirm__message">
            <p>Delete this item?</p>
        </div>
        <div class="pa-popconfirm__actions">
            <button class="pa-btn pa-btn--secondary" onclick="closePopconfirm('popconfirm-compact')">No</button>
            <button class="pa-btn pa-btn--danger" onclick="confirmDelete('popconfirm-compact')">Yes</button>
        </div>
    </div>
</div>

<!-- Compact Popconfirm 2 -->
<div id="popconfirm-compact-2" class="pa-popconfirm pa-popconfirm--bottom pa-popconfirm--compact">
    <div class="pa-popconfirm__arrow"></div>
    <div class="pa-popconfirm__content">
        <div class="pa-popconfirm__message">
            <p>Remove this item?</p>
        </div>
        <div class="pa-popconfirm__actions">
            <button class="pa-btn pa-btn--secondary" onclick="closePopconfirm('popconfirm-compact-2')">No</button>
            <button class="pa-btn pa-btn--danger" onclick="confirmDelete('popconfirm-compact-2')">Yes</button>
        </div>
    </div>
</div>

<!-- Table Popconfirms -->
<div id="popconfirm-table-1" class="pa-popconfirm pa-popconfirm--bottom pa-popconfirm--compact">
    <div class="pa-popconfirm__arrow"></div>
    <div class="pa-popconfirm__content">
        <div class="pa-popconfirm__message">
            <p>Delete John Doe?</p>
        </div>
        <div class="pa-popconfirm__actions">
            <button class="pa-btn pa-btn--secondary" onclick="closePopconfirm('popconfirm-table-1')">No</button>
            <button class="pa-btn pa-btn--danger" onclick="confirmDelete('popconfirm-table-1')">Yes</button>
        </div>
    </div>
</div>

<div id="popconfirm-table-2" class="pa-popconfirm pa-popconfirm--bottom pa-popconfirm--compact">
    <div class="pa-popconfirm__arrow"></div>
    <div class="pa-popconfirm__content">
        <div class="pa-popconfirm__message">
            <p>Delete Jane Smith?</p>
        </div>
        <div class="pa-popconfirm__actions">
            <button class="pa-btn pa-btn--secondary" onclick="closePopconfirm('popconfirm-table-2')">No</button>
            <button class="pa-btn pa-btn--danger" onclick="confirmDelete('popconfirm-table-2')">Yes</button>
        </div>
    </div>
</div>

<div id="popconfirm-table-3" class="pa-popconfirm pa-popconfirm--bottom pa-popconfirm--compact">
    <div class="pa-popconfirm__arrow"></div>
    <div class="pa-popconfirm__content">
        <div class="pa-popconfirm__message">
            <p>Delete Bob Johnson?</p>
        </div>
        <div class="pa-popconfirm__actions">
            <button class="pa-btn pa-btn--secondary" onclick="closePopconfirm('popconfirm-table-3')">No</button>
            <button class="pa-btn pa-btn--danger" onclick="confirmDelete('popconfirm-table-3')">Yes</button>
        </div>
    </div>
</div>

<script>
// Popconfirm positioning and interaction
let currentPopconfirm = null;
let currentTrigger = null;

function togglePopconfirm(event, popconfirmId) {
    event.stopPropagation();

    const trigger = event.currentTarget;
    const popconfirm = document.getElementById(popconfirmId);

    // Close current if different
    if (currentPopconfirm && currentPopconfirm !== popconfirm) {
        currentPopconfirm.classList.remove('is-open');
    }

    // Toggle this popconfirm
    const isOpen = popconfirm.classList.contains('is-open');

    if (isOpen) {
        popconfirm.classList.remove('is-open');
        currentPopconfirm = null;
        currentTrigger = null;
    } else {
        // Position and show
        positionPopconfirm(trigger, popconfirm);
        popconfirm.classList.add('is-open');
        currentPopconfirm = popconfirm;
        currentTrigger = trigger;
    }
}

function closePopconfirm(popconfirmId) {
    const popconfirm = document.getElementById(popconfirmId);
    popconfirm.classList.remove('is-open');

    if (currentPopconfirm === popconfirm) {
        currentPopconfirm = null;
        currentTrigger = null;
    }
}

function confirmDelete(popconfirmId) {
    console.log('Confirmed:', popconfirmId);
    closePopconfirm(popconfirmId);

    // Show toast notification
    const toast = document.createElement('div');
    toast.className = 'pa-toast pa-toast--success';
    toast.textContent = 'Item deleted successfully';
    toast.style.cssText = 'position: fixed; top: 5rem; right: 1rem; z-index: 10000;';
    document.body.appendChild(toast);

    setTimeout(() => {
        toast.remove();
    }, 2000);
}

async function positionPopconfirm(trigger, popconfirm) {
    // The popconfirm's class uses logical sides (start/end). Floating UI only
    // understands physical sides (left/right), so map both ways around its call.
    const isRtl = document.documentElement.dir === 'rtl';
    const toPhysical = {
        top: 'top',
        bottom: 'bottom',
        start: isRtl ? 'right' : 'left',
        end: isRtl ? 'left' : 'right'
    };
    const toLogical = {
        top: 'top',
        bottom: 'bottom',
        left: isRtl ? 'end' : 'start',
        right: isRtl ? 'start' : 'end'
    };

    const classMatch = popconfirm.className.match(/pa-popconfirm--(top|bottom|start|end)/);
    const logical = classMatch ? classMatch[1] : 'bottom';
    const placement = toPhysical[logical];

    // Use Floating UI for automatic positioning with collision detection
    const { computePosition, flip, shift, offset } = window.FloatingUIDOM;

    const { x, y, placement: finalPlacement } = await computePosition(trigger, popconfirm, {
        placement: placement,
        middleware: [
            offset(8),        // 8px gap from trigger
            flip(),           // Flip to opposite side if not enough space
            shift({ padding: 10 })  // Keep 10px from viewport edges
        ]
    });

    // Floating UI may have flipped the side — map its physical answer back to
    // our logical class so RTL stays correct.
    const finalLogical = toLogical[finalPlacement.split('-')[0]] || 'bottom';
    popconfirm.className = popconfirm.className
        .replace(/pa-popconfirm--(top|bottom|start|end)/g, '')
        .trim() + ' pa-popconfirm--' + finalLogical;

    // Apply positioning
    Object.assign(popconfirm.style, {
        left: `${x}px`,
        top: `${y}px`
    });
}

// Close popconfirm when clicking outside
document.addEventListener('click', (e) => {
    if (currentPopconfirm &&
        !currentPopconfirm.contains(e.target) &&
        currentTrigger !== e.target) {
        currentPopconfirm.classList.remove('is-open');
        currentPopconfirm = null;
        currentTrigger = null;
    }
});

// Reposition on scroll/resize
window.addEventListener('scroll', () => {
    if (currentPopconfirm && currentTrigger) {
        positionPopconfirm(currentTrigger, currentPopconfirm);
    }
});

window.addEventListener('resize', () => {
    if (currentPopconfirm && currentTrigger) {
        positionPopconfirm(currentTrigger, currentPopconfirm);
    }
});
</script>
