            <!-- RTL Warning Banner -->
            <div class="pa-alert pa-alert--info mb-base">
                <strong>Note:</strong> This page starts in <strong>RTL</strong> (Right-to-Left) by default —
                it sets <code>dir="rtl"</code> on the HTML element so every component using CSS logical
                properties mirrors automatically (sidebar, toolbars, split-button toggles, icon sides, etc.).
                Use the toggle below to switch to LTR for comparison; the choice is remembered for this
                browsing session and only affects this page (it doesn't change the global demo RTL setting).
                Note the Hebrew text reads right-to-left even in LTR — that's Unicode bidi, independent of
                the layout <code>direction</code>.
            </div>

            <!-- RTL Toggle Control -->
            <div class="pa-card">
                <div class="pa-card__header">
                    <div class="pa-card__title">
                        <h3 class="pa-card__title-text">RTL Toggle / מתג RTL</h3>
                    </div>
                </div>
                <div class="pa-card__body">
                    <div class="pc-row">
                        <div class="pc-col-md-50">
                            <button class="pa-btn pa-btn--primary" id="rtlToggleBtn">
                                <span class="pa-btn__icon"><i class="ti ti-switch-horizontal"></i></span>
                                <span id="rtlBtnLabel">Switch to LTR</span>
                            </button>
                            <span class="ms-base" id="rtlStatus">Current mode: <strong>RTL</strong></span>
                        </div>
                        <div class="pc-col-md-50">
                            <div class="pa-alert pa-alert--info">
                                <strong>How it works:</strong> Setting <code>dir="rtl"</code> on the HTML element flips all components that use CSS logical properties (<code>margin-inline-start</code>, <code>padding-inline-end</code>, etc.). No extra CSS needed.
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Grid System -->
            <div class="pa-card">
                <div class="pa-card__header">
                    <div class="pa-card__title">
                        <h3 class="pa-card__title-text">מערכת הרשת</h3>
                    </div>
                </div>
                <div class="pa-card__body">
                    <p>עמודות הרשת צריכות לשמור על הסדר שלהן אך להתיישר מימין במצב RTL.</p>
                    <div class="pc-row">
                        <div class="pc-col-md-25">
                            <div class="pa-alert pa-alert--primary">עמודה 1 (25%)</div>
                        </div>
                        <div class="pc-col-md-25">
                            <div class="pa-alert pa-alert--success">עמודה 2 (25%)</div>
                        </div>
                        <div class="pc-col-md-50">
                            <div class="pa-alert pa-alert--warning">עמודה 3 (50%)</div>
                        </div>
                    </div>
                    <h4>בדיקת היסט</h4>
                    <div class="pc-row">
                        <div class="pc-col-md-33 pc-offset-md-33">
                            <div class="pa-alert pa-alert--info">היסט 33%, רוחב 33%</div>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Input Groups -->
            <div class="pa-card">
                <div class="pa-card__header">
                    <div class="pa-card__title">
                        <h3 class="pa-card__title-text">קבוצות קלט</h3>
                    </div>
                </div>
                <div class="pa-card__body">
                    <p>התוספות הקדמיות צריכות להופיע בצד ההתחלה (ימין ב-RTL), התוספות האחוריות בצד הסיום (שמאל ב-RTL).</p>
                    <div class="pc-row">
                        <div class="pc-col-md-50">
                            <div class="pa-form-group">
                                <label>שדה עם תוספת קדמית</label>
                                <div class="pa-input-group">
                                    <span class="pa-input-group__prepend">&#8362;</span>
                                    <input type="text" class="pa-input" placeholder="סכום">
                                </div>
                            </div>
                            <div class="pa-form-group">
                                <label>שדה עם תוספת אחורית</label>
                                <div class="pa-input-group">
                                    <input type="text" class="pa-input" placeholder="שם משתמש">
                                    <span class="pa-input-group__append">@example.co.il</span>
                                </div>
                            </div>
                            <div class="pa-form-group">
                                <label>שדה עם שתי תוספות</label>
                                <div class="pa-input-group">
                                    <span class="pa-input-group__prepend">https://</span>
                                    <input type="text" class="pa-input" placeholder="אתר">
                                    <span class="pa-input-group__append">.co.il</span>
                                </div>
                            </div>
                        </div>
                        <div class="pc-col-md-50">
                            <div class="pa-form-group">
                                <label>תוספת + שדה + כפתור</label>
                                <div class="pa-input-group">
                                    <span class="pa-input-group__prepend">חיפוש</span>
                                    <input type="text" class="pa-input" placeholder="מה לחפש...">
                                    <button class="pa-btn pa-btn--primary pa-input-group__button">חפש</button>
                                </div>
                            </div>
                            <div class="pa-form-group">
                                <label>כפתור + שדה + תוספת</label>
                                <div class="pa-input-group">
                                    <button class="pa-btn pa-btn--success pa-input-group__button">בקר</button>
                                    <input type="text" class="pa-input" placeholder="דומיין">
                                    <span class="pa-input-group__append">.co.il</span>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Buttons -->
            <div class="pa-card">
                <div class="pa-card__header">
                    <div class="pa-card__title">
                        <h3 class="pa-card__title-text">כפתורים עם אייקונים</h3>
                    </div>
                </div>
                <div class="pa-card__body">
                    <p>אייקונים צריכים להופיע בצד הנכון ביחס לטקסט במצב RTL.</p>
                    <div class="pa-btn-group">
                        <button class="pa-btn pa-btn--primary">
                            <span class="pa-btn__icon"><i class="ti ti-arrow-right"></i></span>
                            הקודם
                        </button>
                        <button class="pa-btn pa-btn--primary">
                            הבא
                            <span class="pa-btn__icon"><i class="ti ti-arrow-left"></i></span>
                        </button>
                        <button class="pa-btn pa-btn--success">
                            <span class="pa-btn__icon"><i class="ti ti-check"></i></span>
                            שמור
                        </button>
                        <button class="pa-btn pa-btn--danger">
                            <span class="pa-btn__icon"><i class="ti ti-trash"></i></span>
                            מחק
                        </button>
                    </div>
                </div>
            </div>

            <!-- Split Buttons (overflow toolbars in card headers, three-up) -->
            <div class="pa-card">
                <div class="pa-card__header">
                    <div class="pa-card__title">
                        <h3 class="pa-card__title-text">כפתורי פיצול בכותרות כרטיסים</h3>
                    </div>
                </div>
                <div class="pa-card__body">
                    <p>הפעולה הראשית והמתג צריכים להתהפך: הפינות המשוטחות בין שני הכפתורים, קו המפריד, ותפריט הנפתח צריכים ליישר לצד הנכון ב-RTL (Floating UI קורא את כיוון הכתיבה וממקם את <code>bottom-end</code> בצד הסיום). מוצג בשלושה טורים (<code>pc-col-lg-1-3</code>) כך שכל כרטיס כבר צר — שנה את רוחב החלון כדי לראות כל <code>.pa-card__actions--overflow</code> בולע ופולט כפתורי פעולה לתוך תפריט <code>[⋮]</code> ייעודי, כל אחד בנקודת השבירה שלו. כפתור פיצול מתקפל כקבוצה אטומית עם תווית.</p>
                    <div class="pc-row mt-2">
                        <div class="pc-col-100 pc-col-lg-1-3">
                            <div class="pa-card">
                                <div class="pa-card__header">
                                    <div class="pa-card__title minw-45">
                                        <h3 class="pa-card__title-text">לוח ביצועים רבעוני ומעורבות לקוחות</h3>
                                    </div>
                                    <div class="pa-card__actions pa-card__actions--overflow">
                                            <button class="pa-btn pa-btn--xs pa-btn--ghost" title="רענן">
                                                <span class="pa-btn__icon"><span class="pa-icon pa-icon--refresh" aria-hidden="true"></span></span>
                                                רענן
                                            </button>
                                            <button class="pa-btn pa-btn--xs pa-btn--outline-info">
                                                <span class="pa-btn__icon"><span class="pa-icon pa-icon--filter" aria-hidden="true"></span></span>
                                                סנן
                                            </button>
                                            <button class="pa-btn pa-btn--xs pa-btn--outline-secondary">
                                                <span class="pa-btn__icon"><i class="fas fa-gear"></i></span>
                                                הגדר
                                            </button>
                                            <button class="pa-btn pa-btn--xs pa-btn--success" data-pa-actions-priority="10" onclick="togglePopconfirm(event, 'pc-export')">
                                                <span class="pa-btn__icon"><span class="pa-icon pa-icon--download" aria-hidden="true"></span></span>
                                                ייצוא
                                            </button>
                                            <div class="pa-btn-split" data-pa-actions-priority="20">
                                                <button class="pa-btn pa-btn--xs pa-btn--primary">
                                                    <span class="pa-btn__icon"><span class="pa-icon pa-icon--add" aria-hidden="true"></span></span>
                                                    הוסף רכיב
                                                </button>
                                                <button class="pa-btn pa-btn--xs pa-btn--primary pa-btn-split__toggle" onclick="toggleSplitMenu(event)">
                                                    <span class="pa-btn-split__chevron" aria-hidden="true"></span>
                                                </button>
                                                <div class="pa-btn-split__menu">
                                                    <div class="pa-btn-split__menu-inner">
                                                        <button class="pa-btn-split__item">
                                                            <span class="pa-btn-split__item-icon"><i class="fas fa-chart-line"></i></span>
                                                            הוסף תרשים
                                                        </button>
                                                        <button class="pa-btn-split__item">
                                                            <span class="pa-btn-split__item-icon"><i class="fas fa-table"></i></span>
                                                            הוסף טבלה
                                                        </button>
                                                    </div>
                                                </div>
                                            </div>
                                        </div>
                                    </div>
                                <div class="pa-card__body">
                                    <p class="text-muted">סרגל הפעולות בכותרת נושא את התנהגות הגלישה — צמצם את החלון כדי לראות את הגדר, סנן ורענן מתקפלים לתוך תפריט <code>[⋮]</code> הייעודי בסדר זה (עדיפות נמוכה קודם; ייצוא מוצמד; כפתור "הוסף רכיב" שורד הכי הרבה).</p>
                                </div>
                            </div>
                        </div>

                        <div class="pc-col-100 pc-col-lg-1-3">
                            <div class="pa-card">
                                <div class="pa-card__header">
                                    <div class="pa-card__title">
                                        <h3 class="pa-card__title-text">משימות מיגרציה &mdash; עדכוני סכימה בסביבת ייצור</h3>
                                    </div>
                                    <div class="pa-card__actions pa-card__actions--overflow">
                                            <button class="pa-btn pa-btn--xs pa-btn--info">
                                                <span class="pa-btn__icon"><i class="fas fa-circle-check"></i></span>
                                                אמת
                                            </button>
                                            <button class="pa-btn pa-btn--xs pa-btn--warning">
                                                <span class="pa-btn__icon"><i class="fas fa-database"></i></span>
                                                גיבוי
                                            </button>
                                            <button class="pa-btn pa-btn--xs pa-btn--outline-danger" onclick="togglePopconfirm(event, 'pc-rollback')">
                                                <span class="pa-btn__icon"><i class="fas fa-rotate-left"></i></span>
                                                שחזור
                                            </button>
                                            <div class="pa-btn-split" data-pa-actions-priority="20">
                                                <button class="pa-btn pa-btn--xs pa-btn--success">
                                                    <span class="pa-btn__icon"><i class="fas fa-rocket"></i></span>
                                                    פרוס
                                                </button>
                                                <button class="pa-btn pa-btn--xs pa-btn--success pa-btn-split__toggle" onclick="toggleSplitMenu(event)">
                                                    <span class="pa-btn-split__chevron" aria-hidden="true"></span>
                                                </button>
                                                <div class="pa-btn-split__menu">
                                                    <div class="pa-btn-split__menu-inner">
                                                        <button class="pa-btn-split__item">
                                                            <span class="pa-btn-split__item-icon"><i class="fas fa-flask"></i></span>
                                                            הרצת יבש בלבד
                                                        </button>
                                                        <button class="pa-btn-split__item">
                                                            <span class="pa-btn-split__item-icon"><i class="fas fa-vial"></i></span>
                                                            פריסה ל-staging
                                                        </button>
                                                        <button class="pa-btn-split__item pa-btn-split__item--danger">
                                                            <span class="pa-btn-split__item-icon"><i class="fas fa-fire"></i></span>
                                                            פריסה כפויה (דלג על בדיקות)
                                                        </button>
                                                    </div>
                                                </div>
                                            </div>
                                        </div>
                                    </div>
                                <div class="pa-card__body">
                                    <p class="text-muted">ללא הצמדה — האחים מתקפלים בסדר ברירת המחדל (הימני ביותר קודם: שחזור &larr; גיבוי &larr; אמת). כפתור הפריסה שורד הכי הרבה ומתקפל לתפריט <code>[⋮]</code> כקבוצה אטומית עם תווית.</p>
                                </div>
                            </div>
                        </div>

                        <div class="pc-col-100 pc-col-lg-1-3">
                            <div class="pa-card">
                                <div class="pa-card__header">
                                    <div class="pa-card__title minw-60">
                                        <h3 class="pa-card__title-text">ניהול חברי צוות והרשאות &mdash; קונסולת ארגון</h3>
                                    </div>
                                    <div class="pa-card__actions pa-card__actions--overflow">
                                            <button class="pa-btn pa-btn--xs pa-btn--ghost">
                                                <span class="pa-btn__icon"><span class="pa-icon pa-icon--search" aria-hidden="true"></span></span>
                                                חיפוש
                                            </button>
                                            <button class="pa-btn pa-btn--xs pa-btn--outline-secondary">
                                                <span class="pa-btn__icon"><i class="fas fa-arrow-down-wide-short"></i></span>
                                                מיון
                                            </button>
                                            <button class="pa-btn pa-btn--xs pa-btn--outline-info">
                                                <span class="pa-btn__icon"><span class="pa-icon pa-icon--filter" aria-hidden="true"></span></span>
                                                סנן
                                            </button>
                                            <button class="pa-btn pa-btn--xs pa-btn--secondary">
                                                <span class="pa-btn__icon"><i class="fas fa-file-import"></i></span>
                                                ייבוא CSV
                                            </button>
                                            <button class="pa-btn pa-btn--xs pa-btn--info" data-pa-actions-priority="5" onclick="togglePopconfirm(event, 'pc-invite')">
                                                <span class="pa-btn__icon"><i class="fas fa-envelope"></i></span>
                                                הזמן
                                            </button>
                                            <div class="pa-btn-split" data-pa-actions-priority="20">
                                                <button class="pa-btn pa-btn--xs pa-btn--primary">
                                                    <span class="pa-btn__icon"><i class="fas fa-user-plus"></i></span>
                                                    הוסף משתמש
                                                </button>
                                                <button class="pa-btn pa-btn--xs pa-btn--primary pa-btn-split__toggle" onclick="toggleSplitMenu(event)">
                                                    <span class="pa-btn-split__chevron" aria-hidden="true"></span>
                                                </button>
                                                <div class="pa-btn-split__menu">
                                                    <div class="pa-btn-split__menu-inner">
                                                        <button class="pa-btn-split__item">
                                                            <span class="pa-btn-split__item-icon"><i class="fas fa-user-tag"></i></span>
                                                            הוסף משתמש עם תפקיד&hellip;
                                                        </button>
                                                        <button class="pa-btn-split__item">
                                                            <span class="pa-btn-split__item-icon"><i class="fas fa-users"></i></span>
                                                            הוספה מרובה מהצוות
                                                        </button>
                                                    </div>
                                                </div>
                                            </div>
                                        </div>
                                    </div>
                                <div class="pa-card__body">
                                    <p class="text-muted">שישה אחים כאן — הזמן מוצמד בעדיפות 5, כך שהוא שורד את הגל הראשון של הקיפולים אך עדיין מתקפל לפני הוסף משתמש. הכותרת נכנעת ראשונה (מתקצרת עד רצפת רוחב מינימלי) כדי שסרגל הפעולות ישמור על כפתוריו. כפתור "הוסף משתמש" מתקפל לתפריט <code>[⋮]</code> כקבוצה אטומית עם תווית.</p>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Popconfirm definitions for the toolbar buttons above (RTL positioning test) -->
            <!-- Each uses a LOGICAL placement (bottom/start/end); positionPopconfirm
                 maps start/end to the physical side based on dir, so in RTL:
                 start = right, end = left. Toggle LTR to compare. -->
            <div id="pc-export" class="pa-popconfirm pa-popconfirm--bottom">
                <div class="pa-popconfirm__arrow"></div>
                <div class="pa-popconfirm__content">
                    <div class="pa-popconfirm__message pa-popconfirm__icon pa-popconfirm__icon--info">
                        <p>לייצא את כל נתוני הלוח? <code>placement: bottom</code></p>
                    </div>
                    <div class="pa-popconfirm__actions">
                        <button class="pa-btn pa-btn--secondary" onclick="closePopconfirm('pc-export')">ביטול</button>
                        <button class="pa-btn pa-btn--success" onclick="confirmPopconfirm('pc-export', 'הייצוא הושלם')">ייצוא</button>
                    </div>
                </div>
            </div>

            <div id="pc-rollback" class="pa-popconfirm pa-popconfirm--start">
                <div class="pa-popconfirm__arrow"></div>
                <div class="pa-popconfirm__content">
                    <div class="pa-popconfirm__message pa-popconfirm__icon pa-popconfirm__icon--danger">
                        <p>לשחזר את בסיס הנתונים לגיבוי הקודם? פעולה זו אינה הפיכה. <code>placement: start</code> (מימין ב-RTL)</p>
                    </div>
                    <div class="pa-popconfirm__actions">
                        <button class="pa-btn pa-btn--secondary" onclick="closePopconfirm('pc-rollback')">ביטול</button>
                        <button class="pa-btn pa-btn--danger" onclick="confirmPopconfirm('pc-rollback', 'השחזור בוצע')">שחזור</button>
                    </div>
                </div>
            </div>

            <div id="pc-invite" class="pa-popconfirm pa-popconfirm--end">
                <div class="pa-popconfirm__arrow"></div>
                <div class="pa-popconfirm__content">
                    <div class="pa-popconfirm__message pa-popconfirm__icon pa-popconfirm__icon--info">
                        <p>לשלוח הזמנות לכל החברים שנבחרו? <code>placement: end</code> (משמאל ב-RTL)</p>
                    </div>
                    <div class="pa-popconfirm__actions">
                        <button class="pa-btn pa-btn--secondary" onclick="closePopconfirm('pc-invite')">ביטול</button>
                        <button class="pa-btn pa-btn--info" onclick="confirmPopconfirm('pc-invite', 'ההזמנות נשלחו')">שליחה</button>
                    </div>
                </div>
            </div>

            <!-- Tables -->
            <div class="pa-table-card">
                <div class="pa-table-card__header">
                    <h3>טבלאות</h3>
                </div>
                <div class="pa-table-card__body pa-table-card__body--scrollable">
                    <table class="pa-table pa-table--striped pa-table--hover">
                        <thead>
                            <tr>
                                <th>שם</th>
                                <th>דואר אלקטרוני</th>
                                <th>תפקיד</th>
                                <th>סטטוס</th>
                                <th>פעולות</th>
                            </tr>
                        </thead>
                        <tbody>
                            <tr>
                                <td>יוסי כהן</td>
                                <td>yossi@example.co.il</td>
                                <td>מנהל מערכת</td>
                                <td><span class="pa-badge pa-badge--success">פעיל</span></td>
                                <td>
                                    <button class="pa-btn pa-btn--sm pa-btn--primary">ערוך</button>
                                    <button class="pa-btn pa-btn--sm pa-btn--danger">מחק</button>
                                </td>
                            </tr>
                            <tr>
                                <td>רחל לוי</td>
                                <td>rachel@example.co.il</td>
                                <td>משתמש</td>
                                <td><span class="pa-badge pa-badge--warning">ממתין</span></td>
                                <td>
                                    <button class="pa-btn pa-btn--sm pa-btn--primary">ערוך</button>
                                    <button class="pa-btn pa-btn--sm pa-btn--danger">מחק</button>
                                </td>
                            </tr>
                            <tr>
                                <td>דוד ישראלי</td>
                                <td>david@example.co.il</td>
                                <td>עורך</td>
                                <td><span class="pa-badge pa-badge--info">חדש</span></td>
                                <td>
                                    <button class="pa-btn pa-btn--sm pa-btn--primary">ערוך</button>
                                    <button class="pa-btn pa-btn--sm pa-btn--danger">מחק</button>
                                </td>
                            </tr>
                        </tbody>
                    </table>
                </div>
            </div>

            <!-- Alerts -->
            <div class="pa-card">
                <div class="pa-card__header">
                    <div class="pa-card__title">
                        <h3 class="pa-card__title-text">התראות (ניתנות לסגירה)</h3>
                    </div>
                </div>
                <div class="pa-card__body">
                    <p>כפתור הסגירה צריך להופיע בצד הסיום (שמאל ב-RTL).</p>
                    <div class="pa-alert pa-alert--success pa-alert--dismissible">
                        <span class="pa-alert__icon"><i class="ti ti-check"></i></span>
                        <div class="pa-alert__content">
                            <strong>הצלחה!</strong> הפעולה בוצעה בהצלחה. כפתור הסגירה צריך להתהפך ב-RTL.
                        </div>
                        <button class="pa-alert__close"><span class="pa-icon pa-icon--x" aria-hidden="true"></span></button>
                    </div>
                    <div class="pa-alert pa-alert--danger pa-alert--dismissible">
                        <span class="pa-alert__icon"><i class="ti ti-alert-triangle"></i></span>
                        <div class="pa-alert__content">
                            <strong>שגיאה!</strong> משהו השתבש. כפתור הסגירה צריך להיות בשמאל ב-RTL.
                        </div>
                        <button class="pa-alert__close"><span class="pa-icon pa-icon--x" aria-hidden="true"></span></button>
                    </div>
                    <div class="pa-alert pa-alert--warning pa-alert--dismissible">
                        <span class="pa-alert__icon"><i class="ti ti-alert-circle"></i></span>
                        <div class="pa-alert__content">
                            <strong>אזהרה!</strong> שים לב לפרטים הבאים לפני שתמשיך.
                        </div>
                        <button class="pa-alert__close"><span class="pa-icon pa-icon--x" aria-hidden="true"></span></button>
                    </div>
                </div>
            </div>

            <!-- Tabs -->
            <div class="pa-card">
                <div class="pa-card__header">
                    <div class="pa-card__title">
                        <h3 class="pa-card__title-text">לשוניות</h3>
                    </div>
                </div>
                <div class="pa-card__body">
                    <div class="pa-tabs">
                        <button class="pa-tabs__item pa-tabs__item--active" data-tab="tab1">
                            <i class="ti ti-home"></i> בית
                        </button>
                        <button class="pa-tabs__item" data-tab="tab2">
                            <i class="ti ti-user"></i> פרופיל
                        </button>
                        <button class="pa-tabs__item" data-tab="tab3">
                            <i class="ti ti-settings"></i> הגדרות
                        </button>
                    </div>
                    <div class="pa-tabs__content">
                        <div class="pa-tabs__panel pa-tabs__panel--active" id="tab1">
                            <p>תוכן לשונית הבית. הלשוניות צריכות לזרום מימין במצב RTL.</p>
                        </div>
                        <div class="pa-tabs__panel" id="tab2">
                            <p>תוכן לשונית הפרופיל.</p>
                        </div>
                        <div class="pa-tabs__panel" id="tab3">
                            <p>תוכן לשונית ההגדרות.</p>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Vertical Tabs -->
            <div class="pa-card">
                <div class="pa-card__header">
                    <div class="pa-card__title">
                        <h3 class="pa-card__title-text">לשוניות אנכיות</h3>
                    </div>
                </div>
                <div class="pa-card__body">
                    <p>ללשוניות אנכיות צריך להיות גבול בצד הסיום (שמאל ב-RTL).</p>
                    <div class="pa-tabs__vertical-layout">
                        <div class="pa-tabs pa-tabs--vertical">
                            <button class="pa-tabs__item pa-tabs__item--active" data-tab="vtab1">
                                <i class="ti ti-dashboard"></i> לוח בקרה
                            </button>
                            <button class="pa-tabs__item" data-tab="vtab2">
                                <i class="ti ti-chart-bar"></i> אנליטיקה
                            </button>
                            <button class="pa-tabs__item" data-tab="vtab3">
                                <i class="ti ti-report"></i> דוחות
                            </button>
                        </div>
                        <div class="pa-tabs__content" style="flex: 1;">
                            <div class="pa-tabs__panel pa-tabs__panel--active" id="vtab1">
                                <p>תוכן לוח הבקרה. האינדיקטור הפעיל צריך להיות בקצה הימני ב-RTL.</p>
                            </div>
                            <div class="pa-tabs__panel" id="vtab2">
                                <p>תוכן האנליטיקה.</p>
                            </div>
                            <div class="pa-tabs__panel" id="vtab3">
                                <p>תוכן הדוחות.</p>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Tooltips -->
            <div class="pa-card">
                <div class="pa-card__header">
                    <div class="pa-card__title">
                        <h3 class="pa-card__title-text">טיפים צפים</h3>
                    </div>
                </div>
                <div class="pa-card__body">
                    <p>טיפים צפים צריכים להופיע בכיוון הנכון בהתאם לקלאס המשנה.</p>
                    <div class="pa-btn-group gap-base">
                        <button class="pa-btn pa-btn--secondary pa-tooltip" data-tooltip="טיפ למעלה (ברירת מחדל)">
                            למעלה
                        </button>
                        <button class="pa-btn pa-btn--secondary pa-tooltip pa-tooltip--end" data-tooltip="טיפ לסוף">
                            סוף
                        </button>
                        <button class="pa-btn pa-btn--secondary pa-tooltip pa-tooltip--bottom" data-tooltip="טיפ למטה">
                            למטה
                        </button>
                        <button class="pa-btn pa-btn--secondary pa-tooltip pa-tooltip--start" data-tooltip="טיפ להתחלה">
                            התחלה
                        </button>
                    </div>
                </div>
            </div>

            <!-- Spacing Utilities -->
            <div class="pa-card">
                <div class="pa-card__header">
                    <div class="pa-card__title">
                        <h3 class="pa-card__title-text">כלי עזר לריווח לוגי</h3>
                    </div>
                </div>
                <div class="pa-card__body">
                    <p>כלים אלה משתמשים ב-CSS logical properties ומתהפכים במצב RTL.</p>
                    <div class="pc-row">
                        <div class="pc-col-md-50">
                            <h4>שוליים בהתחלה (ms-*)</h4>
                            <div style="background: var(--pc-accent-light); padding: 0.5rem;">
                                <span class="pa-badge pa-badge--primary ms-lg">ms-lg (margin-inline-start)</span>
                            </div>
                            <h4 class="mt-base">ריפוד בהתחלה (ps-*)</h4>
                            <div class="ps-xl" style="background: var(--pc-success-bg-light);">
                                <span class="pa-badge pa-badge--success">ps-xl (padding-inline-start)</span>
                            </div>
                        </div>
                        <div class="pc-col-md-50">
                            <h4>שוליים בסיום (me-*)</h4>
                            <div style="background: var(--pc-accent-light); padding: 0.5rem; text-align: end;">
                                <span class="pa-badge pa-badge--primary me-lg">me-lg (margin-inline-end)</span>
                            </div>
                            <h4 class="mt-base">ריפוד בסיום (pe-*)</h4>
                            <div class="pe-xl" style="background: var(--pc-warning-bg-light); text-align: end;">
                                <span class="pa-badge pa-badge--warning">pe-xl (padding-inline-end)</span>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Text Alignment -->
            <div class="pa-card">
                <div class="pa-card__header">
                    <div class="pa-card__title">
                        <h3 class="pa-card__title-text">כלי עזר ליישור טקסט</h3>
                    </div>
                </div>
                <div class="pa-card__body">
                    <div class="pc-row">
                        <div class="pc-col-md-33">
                            <div class="pa-alert pa-alert--secondary text-start">
                                <code>.text-start</code><br>
                                מיושר להתחלה (ימין ב-RTL)
                            </div>
                        </div>
                        <div class="pc-col-md-33">
                            <div class="pa-alert pa-alert--secondary text-center">
                                <code>.text-center</code><br>
                                תמיד במרכז
                            </div>
                        </div>
                        <div class="pc-col-md-33">
                            <div class="pa-alert pa-alert--secondary text-end">
                                <code>.text-end</code><br>
                                מיושר לסיום (שמאל ב-RTL)
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Forms -->
            <div class="pa-card">
                <div class="pa-card__header">
                    <div class="pa-card__title">
                        <h3 class="pa-card__title-text">טפסים</h3>
                    </div>
                </div>
                <div class="pa-card__body">
                    <form>
                        <div class="pc-row">
                            <div class="pc-col-md-50">
                                <div class="pa-form-group">
                                    <label>שם פרטי</label>
                                    <input type="text" class="pa-input" placeholder="הכנס שם פרטי">
                                </div>
                            </div>
                            <div class="pc-col-md-50">
                                <div class="pa-form-group">
                                    <label>שם משפחה</label>
                                    <input type="text" class="pa-input" placeholder="הכנס שם משפחה">
                                </div>
                            </div>
                        </div>
                        <div class="pa-form-group">
                            <label>כתובת דואר אלקטרוני</label>
                            <input type="email" class="pa-input" placeholder="example@domain.co.il">
                        </div>
                        <div class="pa-form-group">
                            <label>הודעה</label>
                            <textarea class="pa-input" rows="3" placeholder="כתוב את ההודעה שלך כאן..."></textarea>
                        </div>
                        <div class="pa-form-group">
                            <label class="pa-checkbox">
                                <input type="checkbox">
                                <span class="pa-checkbox__label">אני מסכים לתנאי השימוש</span>
                            </label>
                        </div>
                        <div class="pa-btn-group">
                            <button type="submit" class="pa-btn pa-btn--primary">שלח</button>
                            <button type="reset" class="pa-btn pa-btn--secondary">נקה</button>
                        </div>
                    </form>
                </div>
            </div>

            <!-- Toasts -->
            <div class="pa-card">
                <div class="pa-card__header">
                    <div class="pa-card__title">
                        <h3 class="pa-card__title-text">מיקומי הודעות Toast</h3>
                    </div>
                </div>
                <div class="pa-card__body">
                    <p>מיקומי מכולות ה-Toast משתמשים ב-logical properties. לחץ להצגת הודעה במיקומים שונים.</p>
                    <div class="pa-btn-group">
                        <button class="pa-btn pa-btn--secondary" onclick="showTestToast('top-end')">למעלה בסיום</button>
                        <button class="pa-btn pa-btn--secondary" onclick="showTestToast('top-start')">למעלה בהתחלה</button>
                        <button class="pa-btn pa-btn--secondary" onclick="showTestToast('bottom-end')">למטה בסיום</button>
                        <button class="pa-btn pa-btn--secondary" onclick="showTestToast('bottom-start')">למטה בהתחלה</button>
                    </div>
                </div>
            </div>

            <!-- Sample Hebrew Text -->
            <div class="pa-card">
                <div class="pa-card__header">
                    <div class="pa-card__title">
                        <h3 class="pa-card__title-text">טקסט לדוגמה</h3>
                    </div>
                </div>
                <div class="pa-card__body">
                    <p>זהו פסקה לדוגמה בעברית כדי לבדוק את זרימת הטקסט במצב RTL. הטקסט צריך לזרום מימין לשמאל באופן טבעי.</p>
                    <p>Pure Admin תומך ב-RTL באמצעות CSS Logical Properties, מה שמאפשר תמיכה אוטומטית בשפות מימין לשמאל כמו עברית וערבית ללא צורך בסגנונות נפרדים.</p>
                    <div class="pa-callout pa-callout--warning">
                        "הפשטות היא התחכום האולטימטיבי." - לאונרדו דה וינצ'י
                    </div>
                    <h4>רשימה עם נקודות</h4>
                    <ul>
                        <li>פריט ראשון ברשימה</li>
                        <li>פריט שני ברשימה</li>
                        <li>פריט שלישי ברשימה</li>
                    </ul>
                    <h4>רשימה ממוספרת</h4>
                    <ol>
                        <li>שלב ראשון</li>
                        <li>שלב שני</li>
                        <li>שלב שלישי</li>
                    </ol>
                </div>
            </div>

            <!-- Popconfirm logic (RTL positioning test for the toolbar buttons).
                 Mirrors demo/views/popconfirm.mustache — positionPopconfirm maps the
                 logical start/end placement to a physical Floating UI side based on
                 document.documentElement.dir, and maps the flipped result back. -->
            <script>
                let currentPopconfirm = null;
                let currentPopTrigger = null;

                function togglePopconfirm(event, popconfirmId) {
                    event.stopPropagation();
                    const trigger = event.currentTarget;
                    const popconfirm = document.getElementById(popconfirmId);
                    if (!popconfirm) return;

                    if (currentPopconfirm && currentPopconfirm !== popconfirm) {
                        currentPopconfirm.classList.remove('is-open');
                    }
                    if (popconfirm.classList.contains('is-open')) {
                        popconfirm.classList.remove('is-open');
                        currentPopconfirm = null;
                        currentPopTrigger = null;
                    } else {
                        positionPopconfirm(trigger, popconfirm);
                        popconfirm.classList.add('is-open');
                        currentPopconfirm = popconfirm;
                        currentPopTrigger = trigger;
                    }
                }

                function closePopconfirm(popconfirmId) {
                    const popconfirm = document.getElementById(popconfirmId);
                    if (!popconfirm) return;
                    popconfirm.classList.remove('is-open');
                    if (currentPopconfirm === popconfirm) {
                        currentPopconfirm = null;
                        currentPopTrigger = null;
                    }
                }

                function confirmPopconfirm(popconfirmId, message) {
                    closePopconfirm(popconfirmId);
                    // Reuse the page's toast helper (logical bottom-end position).
                    if (typeof showTestToast === 'function') {
                        showTestToast('bottom-end');
                    }
                    console.log('[popconfirm] confirmed:', popconfirmId, '-', message);
                }

                async function positionPopconfirm(trigger, popconfirm) {
                    const isRtl = document.documentElement.getAttribute('dir') === 'rtl';
                    const toPhysical = { top: 'top', bottom: 'bottom', start: isRtl ? 'right' : 'left', end: isRtl ? 'left' : 'right' };
                    const toLogical  = { top: 'top', bottom: 'bottom', left: isRtl ? 'end' : 'start', right: isRtl ? 'start' : 'end' };

                    const classMatch = popconfirm.className.match(/pa-popconfirm--(top|bottom|start|end)/);
                    const logical = classMatch ? classMatch[1] : 'bottom';
                    const placement = toPhysical[logical];

                    const { computePosition, flip, shift, offset } = window.FloatingUIDOM;
                    const { x, y, placement: finalPlacement } = await computePosition(trigger, popconfirm, {
                        placement: placement,
                        middleware: [ offset(8), flip(), shift({ padding: 10 }) ]
                    });

                    const finalLogical = toLogical[finalPlacement.split('-')[0]] || 'bottom';
                    popconfirm.className = popconfirm.className
                        .replace(/pa-popconfirm--(top|bottom|start|end)/g, '')
                        .trim() + ' pa-popconfirm--' + finalLogical;

                    Object.assign(popconfirm.style, { left: `${x}px`, top: `${y}px` });
                }

                document.addEventListener('click', (e) => {
                    if (currentPopconfirm &&
                        !currentPopconfirm.contains(e.target) &&
                        currentPopTrigger !== e.target &&
                        (!currentPopTrigger || !currentPopTrigger.contains(e.target))) {
                        currentPopconfirm.classList.remove('is-open');
                        currentPopconfirm = null;
                        currentPopTrigger = null;
                    }
                });

                ['scroll', 'resize'].forEach((evt) => window.addEventListener(evt, () => {
                    if (currentPopconfirm && currentPopTrigger) {
                        positionPopconfirm(currentPopTrigger, currentPopconfirm);
                    }
                }, true));
            </script>

            <script>
                // --- RTL default for THIS page --------------------------------
                // This is the RTL demo page, so it should ARRIVE in RTL. RTL is
                // otherwise a global demo setting (localStorage 'rtl-mode',
                // applied by settings-panel.js on every page). Here we override
                // it PAGE-SCOPED — the rest of the demo isn't forced into RTL,
                // and the visitor's explicit toggle choice is remembered for the
                // session (sessionStorage), defaulting to RTL when unset.
                const RTL_TEST_KEY = 'rtl-test-dir';

                function updateRtlUI(isRtl) {
                    document.getElementById('rtlStatus').innerHTML = 'Current mode: <strong>' + (isRtl ? 'RTL' : 'LTR') + '</strong>';
                    document.getElementById('rtlBtnLabel').textContent = isRtl ? 'Switch to LTR' : 'Switch to RTL';
                }

                function applyRtl(isRtl) {
                    document.documentElement.setAttribute('dir', isRtl ? 'rtl' : 'ltr');
                    updateRtlUI(isRtl);
                    // Keep the settings-panel checkbox visually in sync while on
                    // this page (does NOT write the global 'rtl-mode' flag).
                    const rtlCheckbox = document.getElementById('rtlMode');
                    if (rtlCheckbox) rtlCheckbox.checked = isRtl;
                }

                document.getElementById('rtlToggleBtn').addEventListener('click', function() {
                    const isRtl = document.documentElement.getAttribute('dir') === 'rtl';
                    const next = !isRtl;
                    sessionStorage.setItem(RTL_TEST_KEY, next ? 'rtl' : 'ltr');
                    applyRtl(next);
                });

                // Just REFLECT the dir into the UI text — settings-panel.js is
                // the single load-time dir authority and already defaulted this
                // page (body[data-rtl-default]) to RTL, honoring any
                // sessionStorage[RTL_TEST_KEY] override, so we don't set dir here.
                // Reflect on `load` (not DOMContentLoaded): handler ordering vs
                // settings-panel isn't guaranteed, and by `load` dir is final.
                window.addEventListener('load', function() {
                    updateRtlUI(document.documentElement.getAttribute('dir') === 'rtl');
                });

                // Simple tab switching for demo
                document.querySelectorAll('.pa-tabs__item').forEach(tab => {
                    tab.addEventListener('click', function() {
                        const tabGroup = this.closest('.pa-card__body');
                        const tabId = this.getAttribute('data-tab');

                        // Deactivate all tabs and panels in this group
                        tabGroup.querySelectorAll('.pa-tabs__item').forEach(t => t.classList.remove('pa-tabs__item--active'));
                        tabGroup.querySelectorAll('.pa-tabs__panel').forEach(p => p.classList.remove('pa-tabs__panel--active'));

                        // Activate clicked tab and corresponding panel
                        this.classList.add('pa-tabs__item--active');
                        const panel = document.getElementById(tabId);
                        if (panel) panel.classList.add('pa-tabs__panel--active');
                    });
                });

                // Toast demo function
                function showTestToast(position) {
                    // Create container if not exists
                    let container = document.querySelector('.pa-toast-container--' + position);
                    if (!container) {
                        container = document.createElement('div');
                        container.className = 'pa-toast-container pa-toast-container--' + position;
                        document.body.appendChild(container);
                    }

                    // Create toast
                    const toast = document.createElement('div');
                    toast.className = 'pa-toast pa-toast--info';
                    toast.innerHTML = `
                        <div class="pa-toast__icon"><i class="ti ti-info-circle"></i></div>
                        <div class="pa-toast__content">
                            <div class="pa-toast__title">מיקום הודעה: ${position}</div>
                            <p class="pa-toast__message">הודעה זו מופיעה במיקום ${position}.</p>
                        </div>
                        <button class="pa-toast__close"><span class="pa-icon pa-icon--x" aria-hidden="true"></span></button>
                    `;

                    container.appendChild(toast);

                    // Trigger animation
                    setTimeout(() => toast.classList.add('pa-toast--show'), 10);

                    // Auto dismiss
                    setTimeout(() => {
                        toast.classList.remove('pa-toast--show');
                        toast.classList.add('pa-toast--hide');
                        setTimeout(() => toast.remove(), 300);
                    }, 3000);

                    // Close button
                    toast.querySelector('.pa-toast__close').addEventListener('click', () => {
                        toast.classList.remove('pa-toast--show');
                        toast.classList.add('pa-toast--hide');
                        setTimeout(() => toast.remove(), 300);
                    });
                }
            </script>
