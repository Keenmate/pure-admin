        <div class="pc-layout-container">
            <p class="mb-6">Resizable container with two or more panes and draggable gutters. Drag to resize, double-click a gutter to collapse / restore, and use arrow keys (or <kbd>Home</kbd> / <kbd>End</kbd>) when a gutter is focused. Sizes can be constrained per pane and persisted to localStorage.</p>

            <!-- Demo 1 — Horizontal -->
            <div class="pa-card mb-4">
                <div class="pa-card__header">
                    <div class="pa-card__title">
                        <h3 class="pa-card__title-text">Horizontal split <small class="text-secondary">— side-by-side, vertical gutter</small></h3>
                    </div>
                </div>
                <div class="pa-card__body">
                    <p class="mb-3">Sidebar + main content. Start pane is constrained to <code>min: 200px</code> and <code>max: 60%</code>, with a default of <code>280px</code>. Size persists to <code>localStorage</code> under the id <code>demo-horizontal</code>.</p>

                    <div class="pa-splitter pa-splitter--horizontal"
                         data-pa-splitter
                         data-pa-splitter-id="demo-horizontal"
                         style="height: 360px; border: 1px solid var(--pc-border-color); border-radius: var(--pc-border-radius);">
                        <div class="pa-splitter__pane"
                             data-pa-splitter-size="280px"
                             data-pa-splitter-min="200px"
                             data-pa-splitter-max="60%"
                             style="background: var(--pc-subtle-bg); padding: 1.6rem;">
                            <h4 class="mb-3" style="margin-top: 0;">Files</h4>
                            <ul class="pa-list-basic">
                                <li>📁 components/</li>
                                <li>📁 utils/</li>
                                <li>📄 main.scss</li>
                                <li>📄 README.md</li>
                                <li>📄 package.json</li>
                                <li>📄 .gitignore</li>
                                <li>📄 LICENSE</li>
                            </ul>
                        </div>
                        <div class="pa-splitter__gutter"
                             role="separator"
                             aria-orientation="vertical"
                             tabindex="0"
                             aria-label="Resize sidebar"></div>
                        <div class="pa-splitter__pane"
                             style="padding: 1.6rem;">
                            <h4 class="mb-3" style="margin-top: 0;">main.scss</h4>
                            <p class="text-secondary mb-3">Drag the gutter, double-click it to collapse, or focus it and use arrow keys.</p>
                            <pre class="pa-code"><code>@use 'variables/index' as *;
@use 'core' as *;
@use 'utilities' as *;

:root {
  @include output-base-css-variables;
  @include output-pa-css-variables;
}</code></pre>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Demo 2 — Vertical -->
            <div class="pa-card mb-4">
                <div class="pa-card__header">
                    <div class="pa-card__title">
                        <h3 class="pa-card__title-text">Vertical split <small class="text-secondary">— stacked, horizontal gutter</small></h3>
                    </div>
                </div>
                <div class="pa-card__body">
                    <p class="mb-3">Editor over console. Constraints: <code>min: 80px</code>, <code>max: 80%</code>, default <code>60%</code>. Persists under <code>demo-vertical</code>.</p>

                    <div class="pa-splitter pa-splitter--vertical"
                         data-pa-splitter
                         data-pa-splitter-id="demo-vertical"
                         style="height: 420px; border: 1px solid var(--pc-border-color); border-radius: var(--pc-border-radius);">
                        <div class="pa-splitter__pane"
                             data-pa-splitter-size="60%"
                             data-pa-splitter-min="80px"
                             data-pa-splitter-max="80%"
                             style="padding: 1.6rem;">
                            <h4 class="mb-3" style="margin-top: 0;">Editor</h4>
                            <pre class="pa-code"><code>function greet(name) {
  return `Hello, ${name}!`;
}

console.log(greet('Pure Admin'));</code></pre>
                        </div>
                        <div class="pa-splitter__gutter"
                             role="separator"
                             aria-orientation="horizontal"
                             tabindex="0"
                             aria-label="Resize editor"></div>
                        <div class="pa-splitter__pane"
                             style="background: var(--pc-subtle-bg); padding: 1.6rem; font-family: var(--pc-font-mono, monospace); font-size: 1.3rem;">
                            <div class="mb-2" style="opacity: 0.6;">$ node demo.js</div>
                            <div>Hello, Pure Admin!</div>
                            <div class="mb-2" style="opacity: 0.6;">$ npm test</div>
                            <div>✓ 42 tests passed</div>
                            <div>✓ 0 failures</div>
                            <div style="opacity: 0.6;">$ <span style="border-right: 1px solid currentColor; animation: pa-blink 1s steps(2) infinite;">&nbsp;</span></div>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Demo 3 — Spaced cards -->
            <div class="pa-card mb-4">
                <div class="pa-card__header">
                    <div class="pa-card__title">
                        <h3 class="pa-card__title-text">Spaced cards <small class="text-secondary">— gutter with breathing room</small></h3>
                    </div>
                </div>
                <div class="pa-card__body">
                    <p class="mb-3">Use native <code>gap</code> on the splitter root to add space between the panes and the gutter — the JS subtracts it from the available space so percent constraints stay accurate. A thicker gutter is opt-in via <code>--pc-splitter-gutter-size</code>.</p>

                    <div class="pa-splitter pa-splitter--horizontal"
                         data-pa-splitter
                         data-pa-splitter-id="demo-spaced"
                         style="height: 280px; gap: 1.6rem; --pc-splitter-gutter-size: 1rem;">
                        <div class="pa-splitter__pane"
                             data-pa-splitter-size="40%"
                             data-pa-splitter-min="25%"
                             data-pa-splitter-max="75%"
                             style="padding: 0;">
                            <div class="pa-card" style="height: 100%; margin: 0;">
                                <div class="pa-card__header"><div class="pa-card__title"><h4 style="margin: 0;" class="pa-card__title-text">Left card</h4></div></div>
                                <div class="pa-card__body">
                                    <p class="mb-0">This pane holds a card. The flex <code>gap</code> on the splitter keeps the card from touching the gutter — no per-pane padding hack needed.</p>
                                </div>
                            </div>
                        </div>
                        <div class="pa-splitter__gutter"
                             role="separator"
                             aria-orientation="vertical"
                             tabindex="0"
                             aria-label="Resize cards"></div>
                        <div class="pa-splitter__pane" style="padding: 0;">
                            <div class="pa-card" style="height: 100%; margin: 0;">
                                <div class="pa-card__header"><div class="pa-card__title"><h4 style="margin: 0;" class="pa-card__title-text">Right card</h4></div></div>
                                <div class="pa-card__body">
                                    <p class="mb-0">Drag the gutter — both cards reflow. The 10px gutter is set inline via <code>--pc-splitter-gutter-size</code>; the default is 6px.</p>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Demo 4 — Minimize to rail -->
            <div class="pa-card mb-4">
                <div class="pa-card__header">
                    <div class="pa-card__title">
                        <h3 class="pa-card__title-text">Minimize to rail <small class="text-secondary">— vertical header instead of collapse</small></h3>
                    </div>
                </div>
                <div class="pa-card__body">
                    <p class="mb-3">Opt in by putting <code>data-pa-splitter-minimize</code> on the pane you want collapsible (here, the first pane). The gutter's collapse action (double-click, <kbd>Enter</kbd>/<kbd>Space</kbd>, or the toggle button in the header) toggles the pane to a thin rail with a vertical card title — instead of disappearing. Click the rail to restore. Rail width is configurable via <code>data-pa-splitter-rail-size</code> (default 40px). <strong>You can also just drag the gutter inward</strong> — once the requested width drops below 40% of the minimized side's natural min (configurable via <code>data-pa-splitter-minimize-threshold</code>) it snaps to rail; dragging back outward across the same threshold pops it back to min. <strong>Dragging the gutter inward while the pane is already rail'd is inert</strong> — only outward drags (the direction that would grow the pane) release the rail.</p>

                    <p class="mb-3 text-sm"><strong>Bonus:</strong> the Editor card's actions use the new <code>pa-card__actions--responsive</code> pattern — drag the gutter rightward to shrink the Editor pane, and once the header drops below <code>$card-actions-collapse-at</code> (28rem ≈ 280px) the spread buttons collapse into a single split button. Pure CSS container query, no JS observer.</p>

                    <label class="mb-3" style="display: inline-flex; align-items: center; gap: 0.6rem; cursor: pointer;">
                        <input type="checkbox"
                               onchange="document.getElementById('demoMinimizeSplitter').classList.toggle('pa-splitter--minimize-mirror', this.checked)">
                        <span>Mirror title (apply <code>transform: scale(-1, -1)</code> to the heading)</span>
                    </label>

                    <div id="demoMinimizeSplitter"
                         class="pa-splitter pa-splitter--horizontal"
                         data-pa-splitter
                         data-pa-splitter-id="demo-minimize"
                         data-pa-splitter-rail-size="44"
                         style="height: 320px; gap: 1.2rem;">
                        <div class="pa-splitter__pane"
                             data-pa-splitter-size="320px"
                             data-pa-splitter-min="220px"
                             data-pa-splitter-max="80%"
                             data-pa-splitter-minimize
                             style="padding: 0;">
                            <div class="pa-card" style="height: 100%; margin: 0;">
                                <div class="pa-card__header">
                                    <div class="pa-card__title">
                                        <i class="pa-card__title-icon fa-solid fa-folder-tree" aria-hidden="true"></i>
                                        <h4 class="pa-card__title-text">File explorer</h4>
                                    </div>
                                    <div class="pa-card__actions">
                                        <button class="pa-btn pa-btn--xs pa-btn--secondary" title="New file" aria-label="New file">
                                            <i class="fa-solid fa-file-circle-plus"></i>
                                        </button>
                                        <button class="pa-btn pa-btn--xs pa-btn--secondary" title="New folder" aria-label="New folder">
                                            <i class="fa-solid fa-folder-plus"></i>
                                        </button>
                                        <button class="pa-btn pa-btn--xs pa-btn--secondary" title="Refresh" aria-label="Refresh">
                                            <span class="pa-icon pa-icon--refresh" aria-hidden="true"></span>
                                        </button>
                                        <button class="pa-btn pa-btn--xs pa-btn--secondary"
                                                data-pa-splitter-toggle
                                                aria-label="Minimize panel"
                                                title="Minimize panel">
                                            <span class="pa-icon pa-icon--chevron-left" aria-hidden="true"></span>
                                        </button>
                                    </div>
                                </div>
                                <div class="pa-card__body">
                                    <ul class="pa-list-basic">
                                        <li>📁 src/</li>
                                        <li>📁 dist/</li>
                                        <li>📄 main.scss</li>
                                        <li>📄 splitter.js</li>
                                        <li>📄 README.md</li>
                                    </ul>
                                    <p class="text-sm text-secondary mt-3 mb-0">Click the chevron in the header to minimize. Click the rail to restore.</p>
                                </div>
                            </div>
                        </div>
                        <div class="pa-splitter__gutter"
                             role="separator"
                             aria-orientation="vertical"
                             tabindex="0"
                             aria-label="Resize or minimize file explorer"></div>
                        <div class="pa-splitter__pane" style="padding: 0;">
                            <div class="pa-card" style="height: 100%; margin: 0;">
                                <div class="pa-card__header">
                                    <div class="pa-card__title">
                                        <i class="pa-card__title-icon fa-solid fa-code" aria-hidden="true"></i>
                                        <h4 class="pa-card__title-text">Editor</h4>
                                    </div>
                                    <div class="pa-card__actions pa-card__actions--responsive">
                                        <div class="pa-card__actions-full">
                                            <button class="pa-btn pa-btn--xs pa-btn--secondary" title="Save" aria-label="Save">
                                                <span class="pa-icon pa-icon--save" aria-hidden="true"></span>
                                            </button>
                                            <button class="pa-btn pa-btn--xs pa-btn--secondary" title="Format" aria-label="Format code">
                                                <i class="fa-solid fa-wand-magic-sparkles"></i>
                                            </button>
                                            <button class="pa-btn pa-btn--xs pa-btn--primary" title="Run" aria-label="Run">
                                                <i class="fa-solid fa-play"></i>
                                            </button>
                                        </div>
                                        <div class="pa-card__actions-collapsed">
                                            <div class="pa-btn-split">
                                                <button class="pa-btn pa-btn--xs pa-btn--primary" title="Run" aria-label="Run">
                                                    <i class="fa-solid fa-play"></i>
                                                </button>
                                                <button class="pa-btn pa-btn--xs pa-btn--primary pa-btn-split__toggle" aria-label="More actions">
                                                    <span class="pa-btn-split__chevron" aria-hidden="true"></span>
                                                </button>
                                                <div class="pa-btn-split__menu">
                                                    <div class="pa-btn-split__menu-inner">
                                                        <button class="pa-btn-split__item" type="button">
                                                            <span class="pa-icon pa-icon--save" aria-hidden="true"></span>
                                                            Save
                                                        </button>
                                                        <button class="pa-btn-split__item" type="button">
                                                            <i class="fa-solid fa-wand-magic-sparkles"></i>
                                                            Format
                                                        </button>
                                                    </div>
                                                </div>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                                <div class="pa-card__body">
                                    <p class="mb-3">When the file explorer minimizes, this card stays put and the gutter slides next to the rail.</p>
                                    <pre class="pa-code"><code>// Toggle the minimize state:
//   - double-click the gutter
//   - focus gutter + press Enter / Space
//   - click the rail (while minimized)
//   - drag the gutter outward past the snap threshold</code></pre>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Demo 4b — Progressive overflow -->
            <div class="pa-card mb-4">
                <div class="pa-card__header">
                    <div class="pa-card__title">
                        <h3 class="pa-card__title-text">Progressive overflow <small class="text-secondary">— buttons drop into a "More" menu one at a time</small></h3>
                    </div>
                </div>
                <div class="pa-card__body">
                    <p class="mb-3">A second collapse model, complementing <code>--responsive</code>. Mark the actions wrapper with <code>pa-card__actions--overflow</code> and the JS module (<code>card-actions-overflow.js</code>) measures each button on init. A <code>ResizeObserver</code> watches the wrapper; when the row can't fit, the lowest-priority button moves into a "..." menu (appended to <code>&lt;body&gt;</code> so card <code>overflow: hidden</code> can't clip it).</p>
                    <ul class="mb-3">
                        <li>Priority via <code>data-pa-actions-priority="N"</code> — higher stays longer.</li>
                        <li>Tiebreak direction via <code>data-pa-actions-overflow-from="end"</code> (default — rightmost drops first) or <code>"start"</code> (leftmost drops first).</li>
                    </ul>

                    <label class="mb-3" style="display: inline-flex; align-items: center; gap: 0.6rem; cursor: pointer;">
                        <input type="checkbox"
                               onchange="document.getElementById('demoOverflowActions').setAttribute('data-pa-actions-overflow-from', this.checked ? 'start' : 'end');">
                        <span>Drop from the start (left) instead of the end (right)</span>
                    </label>

                    <div class="pa-splitter pa-splitter--horizontal"
                         data-pa-splitter
                         data-pa-splitter-id="demo-overflow"
                         data-pa-splitter-rail-size="44"
                         style="height: 220px; gap: 1.2rem;">
                        <div class="pa-splitter__pane"
                             data-pa-splitter-size="40%"
                             data-pa-splitter-min="160px"
                             data-pa-splitter-max="80%"
                             data-pa-splitter-minimize
                             style="padding: 0;">
                            <div class="pa-card" style="height: 100%; margin: 0;">
                                <div class="pa-card__header">
                                    <div class="pa-card__title">
                                        <i class="pa-card__title-icon fa-solid fa-toolbox" aria-hidden="true"></i>
                                        <h4 class="pa-card__title-text">Editor</h4>
                                    </div>
                                    <div id="demoOverflowActions" class="pa-card__actions pa-card__actions--overflow" data-pa-actions-overflow-from="end">
                                        <button class="pa-btn pa-btn--xs pa-btn--secondary" type="button">
                                            <span class="pa-icon pa-icon--save" aria-hidden="true"></span>
                                            <span>Save</span>
                                        </button>
                                        <button class="pa-btn pa-btn--xs pa-btn--secondary" type="button">
                                            <i class="fa-solid fa-wand-magic-sparkles"></i>
                                            <span>Format</span>
                                        </button>
                                        <button class="pa-btn pa-btn--xs pa-btn--secondary" type="button">
                                            <span class="pa-icon pa-icon--refresh" aria-hidden="true"></span>
                                            <span>Refresh</span>
                                        </button>
                                        <button class="pa-btn pa-btn--xs pa-btn--secondary" type="button">
                                            <i class="fa-solid fa-file-export"></i>
                                            <span>Export</span>
                                        </button>
                                        <button class="pa-btn pa-btn--xs pa-btn--primary" type="button" data-pa-actions-priority="10">
                                            <i class="fa-solid fa-play"></i>
                                            <span>Run</span>
                                        </button>
                                    </div>
                                </div>
                                <div class="pa-card__body">
                                    <p class="text-sm text-secondary mb-0">Shrink me — buttons collapse into the "..." menu by ascending priority. <code>Run</code> has <code>data-pa-actions-priority="10"</code> and stays last.</p>
                                </div>
                            </div>
                        </div>
                        <div class="pa-splitter__gutter"
                             role="separator"
                             aria-orientation="vertical"
                             tabindex="0"
                             aria-label="Resize editor"></div>
                        <div class="pa-splitter__pane" style="padding: 0;">
                            <div class="pa-card" style="height: 100%; margin: 0; background: var(--pc-subtle-bg);">
                                <div class="pa-card__body">
                                    <p class="text-sm text-secondary mb-0">Drag the gutter left/right to shrink and grow the editor card. Watch the action row in its header.</p>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Demo 5 — Minimize end pane to rail -->
            <div class="pa-card mb-4">
                <div class="pa-card__header">
                    <div class="pa-card__title">
                        <h3 class="pa-card__title-text">Minimize <em>end</em> pane to rail <small class="text-secondary">— right-edge inspector pattern</small></h3>
                    </div>
                </div>
                <div class="pa-card__body">
                    <p class="mb-3">Put <code>data-pa-splitter-minimize</code> on the last pane to collapse it to a vertical-title rail on the right edge. Same restore paths as collapsing the first pane (rail click, toggle button, dblclick, <kbd>Enter</kbd>/<kbd>Space</kbd>, or drag the gutter outward past the threshold). Useful for inspector / detail / properties panels.</p>

                    <div class="pa-splitter pa-splitter--horizontal"
                         data-pa-splitter
                         data-pa-splitter-id="demo-minimize-end"
                         data-pa-splitter-rail-size="44"
                         style="height: 320px; gap: 1.2rem;">
                        <div class="pa-splitter__pane"
                             data-pa-splitter-size="65%"
                             data-pa-splitter-min="40%"
                             data-pa-splitter-max="80%"
                             style="padding: 0;">
                            <div class="pa-card" style="height: 100%; margin: 0;">
                                <div class="pa-card__header">
                                    <div class="pa-card__title">
                                        <i class="pa-card__title-icon fa-solid fa-code" aria-hidden="true"></i>
                                        <h4 class="pa-card__title-text">Editor</h4>
                                    </div>
                                </div>
                                <div class="pa-card__body">
                                    <p class="mb-3">Drag the gutter to the right (shrinking the inspector) — once the inspector's width drops below 75% of its implied min, it snaps to a rail on the right edge.</p>
                                    <pre class="pa-code"><code>// The inspector on the right is the
// rail-side here. Press the rail, the
// gutter, or the toggle button to
// restore it.</code></pre>
                                </div>
                            </div>
                        </div>
                        <div class="pa-splitter__gutter"
                             role="separator"
                             aria-orientation="vertical"
                             tabindex="0"
                             aria-label="Resize inspector"></div>
                        <div class="pa-splitter__pane"
                             data-pa-splitter-minimize
                             style="padding: 0;">
                            <div class="pa-card" style="height: 100%; margin: 0;">
                                <div class="pa-card__header">
                                    <div class="pa-card__title">
                                        <i class="pa-card__title-icon fa-solid fa-circle-info" aria-hidden="true"></i>
                                        <h4 class="pa-card__title-text">Inspector</h4>
                                    </div>
                                    <div class="pa-card__actions">
                                        <button class="pa-btn pa-btn--xs pa-btn--secondary"
                                                data-pa-splitter-toggle
                                                aria-label="Minimize inspector"
                                                title="Minimize inspector">
                                            <span class="pa-icon pa-icon--chevron-right" aria-hidden="true"></span>
                                        </button>
                                    </div>
                                </div>
                                <div class="pa-card__body">
                                    <p class="text-sm text-secondary mb-0">Properties, outlines, references, etc. live here. Click the chevron, double-click the gutter, or drag the gutter rightward to collapse.</p>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Demo 6 — N-pane (configurable 3–6) -->
            <div class="pa-card mb-4">
                <div class="pa-card__header">
                    <div class="pa-card__title">
                        <h3 class="pa-card__title-text">N panes <small class="text-secondary">— pick a count, both edges minimizable</small></h3>
                    </div>
                    <div class="pa-card__actions" style="display: flex; gap: 0.6rem; align-items: center;">
                        <label for="multiPaneCount" class="text-sm">Panes:</label>
                        <select id="multiPaneCount" class="pa-input pa-input--sm" style="width: auto;">
                            <option value="3" selected>3</option>
                            <option value="4">4</option>
                            <option value="5">5</option>
                            <option value="6">6</option>
                        </select>
                    </div>
                </div>
                <div class="pa-card__body">
                    <p class="mb-3">Per-pane <code>data-pa-splitter-size</code> / <code>-min</code> / <code>-max</code> drive sizing; any pane with <code>data-pa-splitter-minimize</code> can collapse to a rail. Drag any gutter and either neighbour will snap to rail below the threshold. Each pane count remembers its own layout in localStorage.</p>

                    <div id="multiPaneContainer"></div>
                </div>
            </div>

            <!-- Demo 7 — Persistence reset -->
            <div class="pa-card mb-4">
                <div class="pa-card__header">
                    <div class="pa-card__title">
                        <h3 class="pa-card__title-text">localStorage persistence</h3>
                    </div>
                </div>
                <div class="pa-card__body">
                    <p class="mb-3">Both demos above persist their size under <code>pa-splitter:&lt;id&gt;</code>. Reload the page — your drag positions stick. Clear them with the button below.</p>
                    <button class="pa-btn pa-btn--secondary" onclick="clearSplitterStorage()">
                        Clear saved splitter sizes
                    </button>
                    <div id="splitterStorageStatus" class="mt-3 text-sm text-secondary"></div>
                </div>
            </div>

            <div class="pc-row">
                <!-- Markup reference -->
                <div class="pc-col-100 pc-col-lg-50">
                    <div class="pa-card mb-4">
                        <div class="pa-card__header">
                            <div class="pa-card__title">
                                <h3 class="pa-card__title-text">Markup</h3>
                            </div>
                        </div>
                        <div class="pa-card__body">
                            <pre class="pa-code"><code>&lt;div class="pa-splitter pa-splitter--horizontal"
     data-pa-splitter
     data-pa-splitter-id="my-id"&gt;
  &lt;div class="pa-splitter__pane"
       data-pa-splitter-size="240px"
       data-pa-splitter-min="180px"
       data-pa-splitter-minimize&gt;A&lt;/div&gt;
  &lt;div class="pa-splitter__gutter"
       role="separator" tabindex="0"&gt;&lt;/div&gt;
  &lt;div class="pa-splitter__pane"
       data-pa-splitter-min="240px"&gt;B&lt;/div&gt;
  &lt;div class="pa-splitter__gutter"
       role="separator" tabindex="0"&gt;&lt;/div&gt;
  &lt;div class="pa-splitter__pane"
       data-pa-splitter-size="280px"
       data-pa-splitter-minimize&gt;C&lt;/div&gt;
&lt;/div&gt;</code></pre>
                            <p class="text-sm mt-3 mb-0">Any N ≥ 2. Panes and gutters must alternate. Each pane carries its own <code>data-pa-splitter-size</code> / <code>-min</code> / <code>-max</code>. Any pane with <code>data-pa-splitter-minimize</code> can collapse to a rail — first / last panes dock against the container edge; middle panes collapse in place and split released slack between both neighbours. Panes without an explicit <code>data-pa-splitter-size</code> share leftover space equally.</p>
                        </div>
                    </div>
                </div>

                <!-- API -->
                <div class="pc-col-100 pc-col-lg-50">
                    <div class="pa-card mb-4">
                        <div class="pa-card__header">
                            <div class="pa-card__title">
                                <h3 class="pa-card__title-text">Data attributes</h3>
                            </div>
                        </div>
                        <div class="pa-card__body">
                            <p class="text-sm mb-2"><strong>On root:</strong></p>
                            <table class="pa-table pa-table--sm">
                                <thead>
                                    <tr><th>Attribute</th><th>Default</th><th>Description</th></tr>
                                </thead>
                                <tbody>
                                    <tr><td><code>data-pa-splitter</code></td><td>—</td><td>Marker. Required.</td></tr>
                                    <tr><td><code>data-pa-splitter-id</code></td><td>none</td><td>Enables persistence under <code>pa-splitter:&lt;id&gt;</code></td></tr>
                                    <tr><td><code>data-pa-splitter-step</code></td><td><code>10</code></td><td>Keyboard step in px</td></tr>
                                    <tr><td><code>data-pa-splitter-rail-size</code></td><td><code>40</code></td><td>Rail width in px (minimized state)</td></tr>
                                    <tr><td><code>data-pa-splitter-minimize-threshold</code></td><td><code>0.40</code></td><td>Drag snaps to rail below this fraction of the minimized side's natural min</td></tr>
                                </tbody>
                            </table>

                            <p class="text-sm mt-3 mb-2"><strong>On each pane:</strong></p>
                            <table class="pa-table pa-table--sm">
                                <thead>
                                    <tr><th>Attribute</th><th>Default</th><th>Description</th></tr>
                                </thead>
                                <tbody>
                                    <tr><td><code>data-pa-splitter-size</code></td><td>shared</td><td>Initial size (<code>px</code> or <code>%</code>). Unsized panes split the leftover.</td></tr>
                                    <tr><td><code>data-pa-splitter-min</code></td><td><code>0</code></td><td><code>px</code> or <code>%</code></td></tr>
                                    <tr><td><code>data-pa-splitter-max</code></td><td>available</td><td><code>px</code> or <code>%</code></td></tr>
                                    <tr><td><code>data-pa-splitter-minimize</code></td><td>—</td><td>Marker. Pane can collapse to a rail (first / last dock at the edge; middle panes collapse in place).</td></tr>
                                </tbody>
                            </table>

                            <p class="text-sm mt-3 mb-2"><strong>Accordion mode (auto):</strong></p>
                            <p class="text-sm mb-0">When the container is too narrow to fit all panes at their minimums AND there are 2+ minimizable panes, the splitter switches to single-pane-expanded mode — restoring one pane auto-rails the others. Engages and disengages automatically as the container resizes; adds <code>pa-splitter--accordion</code> to the root as a styling hook.</p>
                        </div>
                    </div>

                    <div class="pa-card mb-4">
                        <div class="pa-card__header">
                            <div class="pa-card__title">
                                <h3 class="pa-card__title-text">Keyboard</h3>
                            </div>
                        </div>
                        <div class="pa-card__body">
                            <table class="pa-table pa-table--sm">
                                <thead><tr><th>Key</th><th>Action</th></tr></thead>
                                <tbody>
                                    <tr><td><kbd>←</kbd> <kbd>↑</kbd></td><td>Move boundary toward start (shrink left pane)</td></tr>
                                    <tr><td><kbd>→</kbd> <kbd>↓</kbd></td><td>Move boundary toward end (grow left pane)</td></tr>
                                    <tr><td><kbd>Home</kbd></td><td>Jump left pane to its minimum</td></tr>
                                    <tr><td><kbd>End</kbd></td><td>Jump left pane to its maximum</td></tr>
                                    <tr><td><kbd>Enter</kbd> / <kbd>Space</kbd></td><td>Toggle minimize on the nearest minimizable neighbour</td></tr>
                                </tbody>
                            </table>
                            <p class="text-sm mt-3 mb-0">Each gutter handles its own keyboard input. Tab focuses the next gutter; the "left pane" is whichever pane sits before the focused gutter.</p>
                        </div>
                    </div>

                    <div class="pa-card mb-4">
                        <div class="pa-card__header">
                            <div class="pa-card__title">
                                <h3 class="pa-card__title-text">JavaScript API</h3>
                            </div>
                        </div>
                        <div class="pa-card__body">
                            <p class="mb-2">Auto-initializes on <code>[data-pa-splitter]</code> at <code>DOMContentLoaded</code>. For dynamically inserted splitters:</p>
                            <pre class="pa-code"><code>// Init a single element
pureAdmin.components.splitter.init(document.getElementById('my-splitter'));

// Init all uninitialized splitters in a subtree
pureAdmin.components.splitter.initAll(someContainer);</code></pre>
                            <p class="text-sm mt-3 mb-0"><code>init()</code> is idempotent — calling it twice on the same element is a no-op.</p>
                        </div>
                    </div>
                </div>
            </div>

            <style>
                @keyframes pa-blink {
                    50% { border-color: transparent; }
                }
            </style>

            <script>
                // N-pane demo — rebuild the splitter inside #multiPaneContainer
                // whenever the count changes. Each N persists under its own id
                // so switching back and forth restores per-N layouts.
                (function () {
                    var container = document.getElementById('multiPaneContainer');
                    var select = document.getElementById('multiPaneCount');
                    if (!container || !select) return;

                    var ICONS = ['fa-folder-tree', 'fa-code', 'fa-terminal', 'fa-flask', 'fa-chart-line', 'fa-circle-info'];
                    var FIRST_LAST_ICONS = { first: 'fa-folder-tree', last: 'fa-circle-info' };

                    function buildPane(i, n) {
                        var isFirst = i === 0, isLast = i === n - 1;
                        var pane = document.createElement('div');
                        pane.className = 'pa-splitter__pane';
                        if (isFirst) {
                            pane.setAttribute('data-pa-splitter-size', '220px');
                            pane.setAttribute('data-pa-splitter-min', '160px');
                            pane.setAttribute('data-pa-splitter-max', '360px');
                            pane.setAttribute('data-pa-splitter-minimize', '');
                        } else if (isLast) {
                            pane.setAttribute('data-pa-splitter-size', '240px');
                            pane.setAttribute('data-pa-splitter-min', '180px');
                            pane.setAttribute('data-pa-splitter-max', '380px');
                            pane.setAttribute('data-pa-splitter-minimize', '');
                        } else {
                            pane.setAttribute('data-pa-splitter-min', '160px');
                            // Middle panes can now also collapse to rail
                            // (post-edge-only-restriction lift). The slack
                            // released splits between both neighbours.
                            pane.setAttribute('data-pa-splitter-minimize', '');
                        }

                        var icon = isFirst ? FIRST_LAST_ICONS.first
                            : isLast ? FIRST_LAST_ICONS.last
                            : ICONS[(i % (ICONS.length - 2)) + 1];
                        var title = isFirst ? 'Sidebar'
                            : isLast ? 'Inspector'
                            : 'Workspace ' + i;

                        var actions = '<div class="pa-card__actions"><button class="pa-btn pa-btn--xs pa-btn--secondary" title="Minimize" aria-label="Minimize" data-pa-splitter-toggle><i class="fa-solid fa-window-minimize" aria-hidden="true"></i></button></div>';

                        var body = isFirst
                            ? '<ul class="pa-list-basic"><li>📁 src/</li><li>📁 demo/</li><li>📄 main.scss</li><li>📄 README.md</li></ul>'
                            : isLast
                            ? '<dl><dt>Type</dt><dd>function</dd><dt>Args</dt><dd>name: string</dd><dt>Returns</dt><dd>string</dd></dl>'
                            : '<p class="text-secondary mb-2">Workspace ' + i + ' of ' + (n - 2) + '</p>'
                              + '<pre class="pa-code"><code>// Pane ' + (i + 1) + '\nconsole.log("hi");</code></pre>';

                        pane.innerHTML =
                            '<div class="pa-card" style="height: 100%; margin: 0;">'
                          +   '<div class="pa-card__header">'
                          +     '<div class="pa-card__title">'
                          +       '<i class="pa-card__title-icon fa-solid ' + icon + '" aria-hidden="true"></i>'
                          +       '<h4 class="pa-card__title-text">' + title + '</h4>'
                          +     '</div>'
                          +     actions
                          +   '</div>'
                          +   '<div class="pa-card__body">' + body + '</div>'
                          + '</div>';
                        return pane;
                    }

                    function buildSplitter(n) {
                        container.innerHTML = '';
                        var sp = document.createElement('div');
                        sp.className = 'pa-splitter pa-splitter--horizontal pa-splitter--minimize-mirror';
                        sp.setAttribute('data-pa-splitter', '');
                        sp.setAttribute('data-pa-splitter-id', 'demo-multi-pane-' + n);
                        sp.style.cssText = 'height: 420px; border: 1px solid var(--pc-border-color); border-radius: var(--pc-border-radius); column-gap: 0.8rem; padding: 0.8rem; background: var(--pc-subtle-bg);';

                        for (var i = 0; i < n; i++) {
                            if (i > 0) {
                                var gut = document.createElement('div');
                                gut.className = 'pa-splitter__gutter';
                                gut.setAttribute('role', 'separator');
                                gut.setAttribute('aria-orientation', 'vertical');
                                gut.setAttribute('tabindex', '0');
                                sp.appendChild(gut);
                            }
                            sp.appendChild(buildPane(i, n));
                        }
                        container.appendChild(sp);
                        if (window.pureAdmin.components.splitter) pureAdmin.components.splitter.init(sp);
                    }

                    var COUNT_KEY = 'pa-splitter:multi-pane-count';
                    var saved = parseInt(localStorage.getItem(COUNT_KEY), 10);
                    if (saved >= 3 && saved <= 6) select.value = String(saved);

                    select.addEventListener('change', function () {
                        var n = parseInt(select.value, 10);
                        try { localStorage.setItem(COUNT_KEY, String(n)); } catch (e) { /* quota / privacy mode */ }
                        buildSplitter(n);
                    });
                    buildSplitter(parseInt(select.value, 10));
                })();

                function clearSplitterStorage() {
                    var cleared = 0;
                    for (var i = localStorage.length - 1; i >= 0; i--) {
                        var key = localStorage.key(i);
                        if (key && key.indexOf('pa-splitter:') === 0) {
                            localStorage.removeItem(key);
                            cleared++;
                        }
                    }
                    document.getElementById('splitterStorageStatus').textContent =
                        cleared === 0
                            ? 'No saved splitter sizes found.'
                            : 'Cleared ' + cleared + ' saved size(s). Reload the page to see the defaults.';
                }
            </script>
        </div>
